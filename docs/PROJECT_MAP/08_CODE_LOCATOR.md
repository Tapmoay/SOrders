# 08 代码定位表（需求 → 文件）

<!-- ref-prefix: android/app/src/main/java/com/tapmoay/sorders/ -->
<!-- 声明在文件最顶部：本文的 Kotlin 路径一律省略这个包根（6 层深），
     且**第 1 节的后端行里也会引用 Android 文件**——声明得晚，那些行就解析不到。
     它只是给 check_refs.py **追加**一个候选根，后端路径照旧解析，不受影响。 -->

> **这份文档回答一个问题：改某个功能，该动哪几个文件？**
>
> 用法：接到需求 → 先在表里找对应行 → 直接读「核心」文件。**不要先全库 grep**——同一个业务概念在代码里通常散落 4~14 个文件，grep 只会给你一堆候选，读完才知道哪个是核心。
>
> 与 `01~07` 的分工：那 7 份是**架构视角**（了解"这个项目是什么"），本表是**改动视角**（"我要动这个，去哪个文件"）。两者互补，不重复。
>
> 与 [`08A_ENDPOINT_INDEX.md`](08A_ENDPOINT_INDEX.md) 的分工：**08A 是机器生成的端点索引**（130 个 URL → handler → 精确行号 → 授权），本表不重复它。
> - 问"**某个 URL 落在哪个函数、谁能调**" → 查 08A（grep，别通读）
> - 问"**改这个功能该动哪几个文件**" → 用本表
>
> 08A 有 `--check` 守 freshness，行号比本表准；本表的 `L###` 只是**提示性锚点**，对不上时以符号名为准。
>
> 📏 **本表约 8k tokens 级别**（中文口径估算；**不要通读**——按业务域跳读你关心的那一节）。
> 作为参照：只读 `app/api/v1/orders.py` 一个文件就要约 12k tokens，所以**先看表再定位**始终更省。

---

## 0. 先读这两个文件（最高性价比）

| 文件 | 行数 | 为什么先读它 |
|---|---|---|
| `backend/app/models/enums.py` | 126 | **整个领域词汇表**。`OrderStatus` 五态、`BillingMode`、`LedgerSource`、`OperationAction`（全部审计动作）、`CashFlowBizType`（20+ 收支类型）、`ExpenseCategory`、`ReceiptSettleMode`、`SettlementStatus`… 读完它就知道系统里所有状态和业务分类叫什么。**胜过读 10 个 API 文件** |
| `backend/app/api/v1/router.py` | 50 | 22 个路由模块的挂载清单，一眼看清有哪些业务面 |

### 需要"全仓结构"时：生成签名地图，不要读全部源码

```powershell
cd backend
python -m scripts.code_map . --out map_full.txt      # 2026-09 实测：118 文件 → 约 12.9k tokens
```

产物是「文件 → 类/函数 + 签名 + docstring 首行」，**覆盖 100% 文件**（它不做检索，所以没有"漏掉某个文件"的假阴性）。
以本仓库为参照：`backend/` 全量 **118 文件 / 14,169 行 ≈ 145k tokens** → 签名地图约 **13k tokens**（压缩约 11×，按 token 比；只算 `app/` 时是 99 文件 / 10,703 行 ≈ 110k）。
**改动大、需要通盘看结构时才生成**；日常改功能用本表 + [`08A_ENDPOINT_INDEX.md`](08A_ENDPOINT_INDEX.md) 就够。

> 它是**工具而非提交物**——每次现生成（一秒钟），不要把产物提交进仓库：
> 结构快照会随代码漂，手写文档才需要保鲜，生成物需要的是"随时能重生成"。

---

## 1. 后端（`backend/`）

### 1.1 订单与派单（最大的一块）

| 功能 | 核心文件 | 附带文件 | 注意 |
|---|---|---|---|
| **订单状态流转**（最高频） | `app/services/order_flow.py`（**5 处**：L76 DISPATCHED / L153 CANCELLED / L190 DELIVERED / L229 CANCELLED / L251 PENDING_DISPATCH） | `app/models/enums.py`、**`app/api/v1/orders.py` L686（第 6 处：`driver_ack_view` 的 DISPATCHED→ACCEPTED，即"接单"）**、`app/services/inventory_service.py` | ⚠️ 全后端共 **6 处** `OrderStatus` 赋值，**不是全在 order_flow.py**。改前先确认是否要给 `OrderStatus` 加值 |
| **「哪一档能做什么」的口径**（三端共用） | `app/models/enums.py::OrderStatus`（**五档**）+ `app/services/order_flow.py`（`assign_driver`/`complete_delivery`/`cancel_pending`/`recall_dispatch` 的状态门） + `app/api/v1/orders.py`（`driver_ack_view`/`update_order`/`update_order_freight`/`_payment_scoped_order`） + `app/api/v1/order_products.py::_order_allows_line_edit` | `docs/DOMAIN_MODEL.md` §1.1/§1.2（状态表 + 每档允许动作表）；客户端：H5 `frontend/src/constants/order.ts`、App `android/.../core/OrderStatusModel.kt` | ⚠️ **真源在后端，客户端各写一遍字面量**——真源改了客户端没跟着改，界面**既不报错也不提示**，只是少一个按钮或少一档列表（2026-09-19 审计 H1：H5 整站只有四档，`DISPATCHED` 的订单在司机/派单员端**一个入口都没有**；App 的派单员撤回按钮同样少一档，**派错司机的第一时间撤不回来**）。<br>红线 `_tools/qa/_check_client_contract.py` 从上面这些函数的**源码形状**解析出允许集合，与两端逐值对账；反向验证 `_reverse_verify_client_contract.py`（20 条注入，含「改后端 allowed → 客户端必须红」那条）。<br>⚠️ 加一个新状态时：`enums.py` → `DOMAIN_MODEL.md` → 两端模型 → 每个角色的列表入口，四处都要动 |
| **订单流转的库存联动**（极易漏） | `app/services/inventory_service.py` | `app/services/order_flow.py`、`app/api/v1/order_products.py` | 状态机的每一步都在联动库存：派单→`auto_stock_out`（预占）、送达→`auto_stock_commit`（真扣，**SQL 表达式自减**）、撤销/撤回→`auto_stock_release`；**订单行改了**（加/改/删行）→ `order_products._resync_stock_if_assigned` → `resync_reservations`（按商品逐一对账补差额）。<br>⚠️ **"派过单没有"的判据是 `order.dispatched_at`，不是"有没有 RESERVED 流水"**：`auto_stock_out` 是**逐行**写的，商品解析不出来的行（手输商品名 / 多个同名）一条流水都没有 —— 用流水当判据会让"派单后改行绑上商品"的单**预占永远补不回来**、送达**一件都不扣**（2026-09-19 审计实测：在途单里 21 个 (单,商品) 对零预占）。红线 `_tools/qa/_check_inventory_reservation.py`，回归测试 `tests/test_audit_round17_reservations.py`。<br>⚠️ **计数列只能由数据库自增**（`stock = stock + delta`），不许 Python 读改写（lost update）；红线 `_tools/qa/_check_counter_updates.py`。<br>⚠️ 库存不足的判据必须写进 `UPDATE ... WHERE`（写入那一刻再判），否则并发下拦不住（TOCTOU） |
| **派单 / 批量派单 / 撤回** | `app/api/v1/orders.py`（L281 `batch_assign_orders`、L729 `assign_order`、L996 `recall_order`） | `order_flow.py::assign_driver` / `recall_dispatch` | 撤回会写订单快照日志，见 `order_snapshot_for_log` |
| **司机送达 + 收现金/挂账** | `app/services/order_flow.py::complete_delivery` | `app/api/v1/orders.py`（L739 `complete_order_with_upload`＝拍照送达、L1214 `complete_order`＝JSON 送达、L1132 `_apply_complete_payment`、**L1187 `_apply_complete_payment_logged`**＝收款处理 **+ 留痕**，两条送达路径共用这一处）、`app/services/accounting_service.py` | 送达会**同步货主账本 + 触发司机应付 + 货损记账**，不是单纯改状态。<br>⚠️ **两条送达路径的收款处理只许有一处**（2026-09-19 报告 P1-10）：`complete-with-upload` 曾漏传第一个位置参数 → `TypeError`（`except ValueError` 接不住）→ **恒 500**，而 `complete_delivery` 已经改状态/扣库存/入账，只有 `db.commit()` 没跑（照片却已落盘）。所以不要在两个端点里各写一遍收款逻辑。<br>⚠️ **拍照送达把文件写到公开静态目录之前，必须先判归属与状态**（报告 P1-11）：同文件 `upload_delivery_photos` 是做对的那个对照组（三道门都在落盘前）。<br>⚠️ **送达照片必须是本系统上传端点的产物形状**（`/static/uploads/delivery/…`，报告 L-14）：判据在 `order_flow.complete_delivery` 里 —— 原来只判"列表非空"，传一个字符 `x` 就满足拍照义务。**刻意只认前缀、不碰文件系统**（收紧到"存在且属于本单"会牵动 15 份用假 URL 的测试夹具与历史数据） |
| **拆单** | `app/services/order_flow.py::split_order` | `app/api/v1/orders.py` L759 `split_order_endpoint` | 数量按比例拆分，**余数归首份** |
| **下单 / 订单行** | `app/api/v1/orders.py`（L359 `create_order` 建单） | `app/services/order_flow.py::build_order_products`、`app/schemas/order.py`、`app/api/v1/order_products.py` | 商品**成本快照在下单时定格**，货损/毛利率按此成本算。<br>⚠️ **订单行单位**（`order_products.unit_snapshot`，出参名叫 `unit`）也在这条链上：客户端不传时回退**商品库里的单位**，拆单时**跟着拆**。出参名 ≠ 列名，`OrderProductOut.unit` 必须写 `validation_alias=AliasChoices(...)` —— 不写**不报错、只是恒为空串**（表现是"选了 3 箱，详情页只剩 3"） |
| **订单软删除 / 恢复** | `app/api/v1/orders.py`（**L334** `delete_cancelled_order` 软删、**L546** `restore_order` 恢复；⚠️ 同一文件里**还有第二个同路径的 `DELETE /{order_id}`**，被 L334 抢先匹配、永不生效——以实测为准，别信 OpenAPI 的 operationId） | `app/services/data_retention.py` | 软删 **30 天**隔离（`data_retention.py` L28 `SOFT_DELETE_RETENTION_DAYS`），物理清理在同一文件 |
| ⚠️ **不是**订单保留策略 | — | ~~`app/services/cancelled_order_retention.py`~~ | **该文件是死代码**（全后端零导入）。且它 L18 的 `CANCELLED_ORDER_RETENTION_DAYS = 10` 与现行 **30 天**策略**数值冲突**——照它回答会答成 10 天 |
| **订单异常** | `app/api/v1/orders.py` L484 `patch_order_exception` | — | — |
| **订单出参装配**（加**派生**字段才看） | `app/services/order_response.py::enrich_order_out` | `app/api/v1/orders.py`（**19 处调用**，唯一调用方） | ⚠️ **加普通列不用动这个文件**——它 L28 是 `OrderOut.model_validate(order)`，**自动带出**模型上有、`OrderOut` 上也有的字段。只有**派生字段**（要靠别的表/按角色算出来的：`driver_phone`/`driver_name`/`driver_billing_mode`/`shipper_name`/`is_new_for_driver`/`freight_visible`/`internal_notes`）才要在这里手写。<br>真正的坑在**别漏 `OrderOut`**：模型有列但 Out 没字段 → `model_validate` 拿不到 → **静默不返回、不报错**。<br>⚠️ `app/api/v1/freight_settlement.py` L13 有一行 `import enrich_order_out as _enrich  # noqa: F401`——**那是未使用的死导入，不是调用点** |
| **账本出参装配**（加字段必看） | `app/services/ledger_response.py`：单行 `ledger_to_out(row, db)`（**3 处调用** L318/L337/L442）、列表 `ledger_rows_to_out(rows, db)`（`GET /ledger/entries` 用） | `app/api/v1/ledger.py` | ⚠️ **这个才是逐字段手写映射**（L10-L38 的 `_to_out` 一个个 `id=row.id, …`）——给 `ledgers` 表加列，**必须在这里补一行**，否则静默丢失。<br>⚠️ **列表端点不许用逐行 `ledger_to_out`**（2026-09-19 外部完整检查 C-4）：`db.get()` 每次都真发一条 SQL（身份映射对已加载对象持**弱引用**），实测 85,474 行 → 85,476 条 SQL / 27.75 秒。`ledger_rows_to_out` 一次 `IN` 取订单，SQL 条数变**常数 2** |
| **给表加字段**（跨层配方，极易漏） | ① `app/models/<表>.py` ② **`app/core/schema_bootstrap.py`**（手写 `ALTER TABLE`，共 66 处） ③ 对应 `app/schemas/`（**`OrderOut` 漏字段 = 静默不返回**） ④ 仅派生字段才动 `order_response.py`（见上两行） ⑤ **若该字段要下发到 App → Android 三层**：`data/remote/dto/Dtos.kt`（DTO + 提交体，**必须给默认值**）→ `data/remote/api/Apis.kt`（`@Part`/body）→ `data/repo/AppRepository.kt`（透传），**新字段若要在界面输入/展示，再加 ViewModel + Screen** | `frontend/`（旧 H5，可选） | ⚠️ **本项目没有 Alembic**，迁移全在 schema_bootstrap 手写，且在 `app/database.py` L42 **`import` 时执行**。漏第 ② 步 = **本地 SQLite 能跑、生产 MySQL 500**。<br>⚠️ 第 ⑤ 步**实测漏过**：跨层字段必然连带 3 个 Android 文件（`Dtos`/`Apis`/`AppRepository`），要输入还要 +2（ViewModel/Screen）；旧 H5 是位置参数，不改不报错但永远传空 |
| **加一个接口**（配方） | ① `app/api/v1/<模块>.py` 加 `@router.<verb>` 处理函数 ② `app/schemas/` 加 In/Out ③ 若需新权限 → 见下一行 ④ 出参走 `enrich_order_out` 而非手拼（订单类） ⑤ **若要给 App 调用 → Android 三层**：`data/remote/api/Apis.kt`（Retrofit 方法）→ `data/remote/dto/Dtos.kt`（出入参，**必须给默认值**）→ `data/repo/AppRepository.kt`（透传）；要上界面再加 ViewModel/Screen | `app/api/v1/router.py`（仅新模块才需挂载）、旧 H5 `frontend/src/api/` | 现有模块加端点**不用动 router.py**；只有新增模块文件才要 `include_router`。<br>⚠️ 第 ⑤ 步**实测漏过**（与「给表加字段」是同一个坑）：后端加完接口，App 侧不会自动有——不补 Android 三层，接口等于只对旧 H5 和 curl 可见 |
| **加一个权限点**（配方） | ① `app/core/rbac.py`（`Permission` 枚举 + `ROLE_PERMISSIONS` 表）② 端点签名改用 `require_permission(...)` | [`08A_ENDPOINT_INDEX.md`](08A_ENDPOINT_INDEX.md)「权限点反查」段 | ⚠️ **看 `rbac.py` 推不出某端点的实际准入范围**——130 个端点里大量只用 `CurrentUser`（仅要求登录），并未挂 `require_permission`。端点到权限的映射查 08A，不要自己从 `ROLE_PERMISSIONS` 反推 |
| **AI 助手**（派单员 + 货主；司机不给。**读 + 申请式写**） | **Android**：`ai/AiAgentLoop.kt`（工具调用循环，**硬上限 8 轮**；system prompt 也在这）、`ai/AiTools.kt`（**8 个工具**：6 读 + 记忆 + **申请式写**；出参裁剪，裁剪逻辑已统一到 `AiRowShaper`；**行数上限 `MAX_ROWS=200`/`DEFAULT_ROWS=20`**——v3.34b 前是 50，导致 97 个货主/62 个司机**物理上列不全**，见 §49）、`ai/AiAnswerSanitizer.kt`（**回答净化器**：编号兜底）、`ai/AiMarkdown.kt`（**模型答复的 Markdown 解析**：表格/加粗/列表，纯逻辑）、`ai/AiContext.kt`（**上下文预算**：**主触发点是固定预算 `HISTORY_BUDGET_TOKENS`=8k、与窗口无关**（v3.34 改；旧口径「窗口 40%」在 1M 窗口下等于永不触发，见 §48）+ **先降级再丢弃**的分级保真 `fitToBudget`（最近 6 条不动 / 旧回答压成首行 / 附件块从尾巴截断保住用户那句话）+ 窗口**按模型名自动推断**（deepseek 实测 1,048,576）+ **双向自愈**（撞上限读端点声明的上限、贴着自己上限成功则翻倍）；窗口比例 40%/90% 降级为兜底）、`ai/AiCompactor.kt`（**上下文压缩**：较早对话→摘要，失败如实降级；**由 `AiChatViewModel.scheduleCompaction` 在回答落地后后台调用**，不再挡用户这一轮）、`ai/AiHabit.kt` + `ai/AiHabitStore.kt`（**使用习惯**：本机统计 + 注入纪律 + 可清除）、`ai/ThinkingLevel.kt`（**思考强度四档** + 老布尔开关迁移）、`ai/LlmClient.kt`（直连 OpenAI 兼容 `/chat/completions`；**端点不认 `thinking` 时自动去掉该参数重试并记住**）、`ai/AiProviders.kt`（厂商预设：DeepSeek/豆包/千问）、`ai/AiKeyStore.kt`（**平台 Keystore** 存用户自带 key + 能力缓存 + 模型候选）、`ai/AiConversation.kt`（对话模型 + 分支/复制/上限/合并，**纯逻辑无 Android 依赖**）、`ai/AiConversationStore.kt`（历史落盘：私有目录 + 原子写 + 坏文件留证 + **没读过盘时只合并不覆盖**）、`ui/ai/AiChatViewModel.kt`、`ui/ai/AiChatScreen.kt`（**左侧历史抽屉 + 模型栏 + 消息上复制/分支图标 + 分支横杠 + 输入区附件面板**：常态只有「⊕ + 输入框 + ↑」，点 ⊕ 展开成 **最近相册照片横排（点一下就挂）+ 拍照/相册/文件 三键**，见 §50）、`ai/AiRecentPhotos.kt`（**最近照片**：只读 MediaStore、没权限就返回空表、不碰其它字段）、`ui/ai/AiRichText.kt`（**表格画成真表格**）、**附件三件套**：`ai/AiAttachment.kt`（**挂载文件的提示词拼装**：TSV 原文 + 截断必须说出来 + 找不到制表符串列；纯逻辑有单测）、`ai/AiAttachmentLoader.kt`（SAF URI → 字节；**图片走相册**并压到长边 1280）、`ai/AiVision.kt`（**能不能看图**：认得的写死、明显非对话模型拦住、其余放行；真正兜底在 LlmClient 的"图被拒→去掉图重试"）、`ai/AiAttachmentService.kt`（上传给后端读成文本表格）、`ai/AiTable.kt`（**拆行拆列的唯一一份规则**，调价表与商品表共用）、`ui/ai/AiSettingsScreen.kt`、**`ai/AiReadService.kt` + `ai/AiReadCatalog.kt`（机器生成）+ `ai/AiRowShaper.kt`（**读所有列表**：通用读工具 `read_data` + 40 条编译期白名单 + 编号只在 App 侧解析 + 出参加工的唯一出口）** | `ai/AiContainer.kt`（组装入口）、`ui/nav/Modules.kt`（工作台入口配置——**货主端 AI 在工作台最后一格**，带 `gradient` 字段；`AiNavButton` 尺寸常量）、`ui/theme/AiBrand.kt`（**AI 品牌渐变（Google AI 三色）的唯一定义处**）、`ai/AiWrite.kt` + `ai/AiWriteService.kt` + `ai/AiWrite*Handlers.kt`（**申请式写**：风险分级 + 两段式确认 + 声明式 CRUD，见 §20~§28）、`_tools/ai/_write_coverage.py`（**覆盖率**：后端 74 个写端点哪些已有动作，链路是 3 跳的，靠它算别靠回忆）、`_tools/ai/_check_order.py`（真机 E2E 之后在库里对账）、`_tools/ai/_check_ledger.py` + `_check_receipt.py` + `_counts.py` + `_show_local_samples.py`（账本/收款/行数快照与料表）、`ai/AiReads.kt`（**读能力的角色裁剪**，与 `AiWrites.forRole` 对称）、`ai/AiScope.kt`（三份本机数据按用户分区）、`ui/home/RoleHomeScreen.kt`（**派单端 AI 入口 = 底部导航正中间的凸起圆钮**；货主端 3 个 Tab 放不正中，改走工作台网格）、`ui/nav/{Routes,NavGraph}.kt`（`ai/chat`、`ai/settings` 两条路由）、`_tools/ai/`（工具白名单生成/校验、`_check_ai_guardrails.py` 红线静态检查、`_seed_test_data.py` 成套造数、`_check_agg_after_seed.py` 接口 vs 库直查对账）、`android/.../ai/{AiAgentLoopTest,LlmClientRequestTest,AiAnswerSanitizerTest,AiConversationsTest,AiMarkdownTest,AiContextTest,AiHabitTest,AiCompactorTest}.kt`（**140 个纯 JVM 单测**） | ⚠️ 十条硬规矩：<br>① **AI 无特权**——用登录 token 走同一套 RBAC；但 `rbac.py:109-113` 对派单员**一律放行**，所以权限收敛**靠工具白名单，不靠提示词**；<br>② 工具响应必须**剪裁 + 剔除成本字段**（`cost_price`/`cost_total`/`cost_covered_lines`/`毛利`）；<br>③ **写能力只有一条路径**：模型只能调 `preview_write` **申请**，落库的 `AiWriteService.execute` 只被聊天页的确认按钮调用；token 由 App 生成、从不进提示词与工具返回值（**不是"注册后禁用提示词"**）；<br>④ **回答里绝不出现内部编号**：三层防线=工具出参不给 `*_id`（`isHiddenField`）＋名字兜底（`safeName`）＋出口净化（`AiAnswerSanitizer`）。**任何一层被删都会漏**；<br>⑤ **对话历史落盘**（私有 `filesDir`）且必须有硬上限（50 段/200 条/单条 8000 字/盘上 2MB），写盘必须**先写 `.tmp` 再 rename**；<br>⑥ ⚠️ **读盘完成前绝不允许落盘**（`AiChatViewModel.loaded`）——`all` 是异步读进来的，读进来之前按它算会**把盘上原有对话覆盖成只剩当前这一段**（实测丢过一次）。存储层还有第二道：没读过盘时 `save()` 退化成按 id 合并；<br>⑦ **表格必须真渲染**（`AiRichText`）+ 列宽按内容给最小宽度、装不下横向滚；system prompt 另有「表格最多 4 列」的硬要求；<br>⑧ **上下文**：历史不再固定只带 10 条（要能用满窗口）→ 到 **40% 自动压缩**（`AiCompactor`）→ 压缩失败还有 **90% 硬裁**（`AiContext.trimToFit`）。用量取**服务端 `prompt_tokens`**，不自己数；窗口**不给用户选**（`/models` 不返回窗口大小），因此**必须有双向自愈链**：认出「上下文超长」→ **优先读端点自己报的上限**（`parseStatedLimit`，实测报文里写着 `maximum context length is 1048576 tokens`）→ 否则按实证值收缩 → 按模型记住（`AiKeyStore.rememberWindow`）→ 最多重试 2 次；反向「猜小了」由 `growOnEvidence`（贴着自设上限成功一次就翻倍）负责。判据只用关键短语，**裸 `exceed`/`token limit` 是禁用的**（限流/欠费会被误判成超长，白缩窗口还会把错误结论记到模型头上）。⚠️ **家族窗口表里的数必须实测**（`_tools/ai/_probe_context_overflow.py`），凭印象写过 128k、实测 1,048,576，差 8 倍；<br>⑨ **思考强度分档**会下发 `reasoning_effort`，但它**在部分端点上被静默忽略**（本项目实测过）——界面必须如实标注，不许暗示"选高一定更准"。老布尔开关要迁移（否则等于偷偷把花费翻倍）；<br>⑩ **使用习惯会改变模型默认行为**，所以：样本 <3 次不注入、注入时必须写明"只在用户没说清时用"+"必须说明按什么范围统计"、设置页要把学到什么摊开给用户看并能一键清除。<br>⑪ `api_base_url` 指向哪，AI 工具就打到哪——**生产后端没有这些新端点时会 404**。<br>⑫ **通用读工具（`read_data`）**：把 40 张只读列表开放给 AI，但 ① **路径只能从编译期白名单 `AiReadCatalog` 查**（机器生成，改它要重跑 `_gen_ai_read_catalog.py`；改窗口/端点表前先看 `--check`）② 只收**路径里没有 `{}`** 的端点（按 id 查详情不开放，AI 看不到编号）③ 需要编号的筛选条件**按名字在 App 侧解析**（`AiReadService.resolveId`），编号全程不进模型上下文 ④ `extra` 里塞编号是**硬错误**（忽略 = 会跑出"全部数据"当"这个商品的"报给用户）⑤ 不支持的筛选条件与替它补的必填项必须写进 `ignored_filters`/`assumed_filters` ⑥ 出参一律过 `AiRowShaper.shape`（**唯一出口**，别再抄第二份剔字段逻辑）⑦ 两层开关：工具级 + 模块级（设置页）⑧ ⚠️ **路径必须取完整路径**（`@router.get("/summary")` 是相对路由前缀的，直接发会打到 `/api/v1/` 吃 404）。<br>⑬ **顶栏形态**：`AppTopBar` 有副标题时是两行、**没有副标题时是单行**（标题与 `subtitleTrailing` 同一行并垂直居中）；⚠️ **不要在 `actions` 里放带 `weight` 的子项**——那会把标题挤成 0 宽（实测标题直接消失）。<br>⑭ **消息上的动作**：用户消息 = 复制 + **编辑**（`beginEdit` 装回输入框，发送时 `applyPendingEdit` 先撤掉这条及其之后的内容再重跑；**绝对不要加「重问」**，用户明确否掉）；模型回答 = 复制 + **分支**（另存一份，原对话保留）。编辑的连带项：`totalTokens` 重算、摘要若覆盖了被撤内容要清掉、`contextUsed` 归零、切对话时清 `editingIndex`。⑮ **读能力也按角色裁剪**：`AiReadCatalog.ReadAction.roles` 是**机器生成**的（`_tools/ai/_gen_ai_read_catalog.py` 从后端源码的权限点/体内 403 门槛推导），三处同源——工具说明、enum、执行前的门（`AiReads.allows`）。改后端鉴权后先跑 `_tools/ai/_probe_read_roles.py`（对三个角色逐条打真后端对账，40×3 全一致才算数）。⚠️ 裁多了（明明能查却说没权限）和裁少了（问一句撞 403）**都是能力缺失**，所以单测是双向的。<br>配套缺口端点：`GET /users?q=`、`GET /stats/shipper-performance`、`GET /inventory/summary?below_alert=true`。<br>方案与实测记录（含两条待拍板发现）：`docs/AI_ASSISTANT_PLAN_V3.md` §13 |

> `app/api/v1/orders.py` 共 **1165 行 / 23 个端点**，是后端最大的单文件。改动前先用它内部的行号定位，不要整文件读。

### 1.2 账务、结算、报表

| 功能 | 核心文件 | 附带文件 | 注意 |
|---|---|---|---|
| **账本流水 / 手动记账** | `app/api/v1/ledger.py`（751 行） | `app/models/ledger.py`、`app/services/ledger_sync.py` | 下单/送达会**自动写账**；改账本前先看 `ledger_sync.py`。<br>📱 **App 侧「记一笔账」的规矩**（商品只能从商品库选、货主可选可填、单价可改但改过不许被拽走、提交不传 `total`）见下面「账本管理」那一行；`POST /ledger/entries` 本来就收 `product_id` + 二选一的货主，所以那一轮后端一个字节没动。<br>⚠️ **回写订单行只对 `source=ORDER` 生效**（2026-09-19 审计 F2）：`REFUND`（货损红冲）行的数量/单价/金额记的是**被冲掉的那一部分**（常常是 qty=2/单价=0/金额=0），让它也回写的话"改一条红冲流水的备注"就会把订单商品行**静默清零**（该单营业额掉到 0，而司机账单与 ORDER 账行不动 = 一张单三个数）。<br>⚠️ **账本行不许改挂到别的订单**（F3）：`_reject_if_order_closed` 按改**之前**的 `order_id` 判，而回写按**新的** —— 改挂等于绕过订单状态守卫。要纠正挂错的账请删掉重记。<br>⚠️ 明细（数量/单价/金额）只对 `source in (MANUAL, ORDER)` 开放；`REFUND` 只能改备注 |
| **客户收款 / 核销** | `app/services/accounting_service.py::create_receipt`、`app/api/v1/ledger.py`（`POST/GET /ledger/receipts`） | `app/models/shipper_receipt.py`、`app/models/cash_flow.py` | 两种口径：`itemized` 逐单核销（必须绑单、amount 必须**逐分等于**各单该收的钱之和、**逐单**各写一条现金流水）与 `rolling` 滚动收款（不绑单、**一笔**流水、不把任何单标已收）。<br>⚠️ **「该收多少」只有一处算**（`services/order_money.py`）：整单核销 = 这一单的**欠款**（`arrears`，收过一部分的单要能收剩下那部分）、按商品核销 = 点名那几行的应收（`line_receivable`）。写成 `receivable`（当时卖了多少）的后果很具体：按商品核销过一部分的单（欠 700/应收 1000）两边金额对不上 → **这种单再也收不动**；凑巧对上就是**多收 300**。<br>⚠️ **一张收款单可以带多张单**（账本页「点合计 → 核销全部」）：一张凭证 + 逐单各一条流水。<br>⚠️ **绑了单就必须对得上**（两种模式同一条底线，R12-M4）。<br>⚠️ **已撤销 / 已软删的单不许收款**（2026-09-19 审计 F1b：本机曾有 73 张已撤销单挂着收款单 + 流水 ¥2,900）；**预收（还没送达就先收钱）是产品口径**，不在这道门槛里。<br>⚠️ **`paid` 只认一次**：送达时 `_apply_complete_payment` 会先问"这单收过款没有"（`order.paid` 或指向它的 inbound 流水），收过就不许改回未收（否则这张单能被再核销一次 = 资金流入翻倍，F1）。<br>⚠️ 收款**目前没有作废/红冲入口** → 见台账「待拍板」第 11 条 |
| **批发商自记账核销**（货主端「我的账本」给下游货主收钱，2026-09-20） | `app/api/v1/shipper_ledger.py`（`GET/POST /shipper-ledger/settlements`、`DELETE …/{id}`、`POST …/{id}/restore`）、`app/services/shipper_settle.py`（**口径只有这一处**：行应收 − 已核销 = 还可核销） | `app/models/shipper_settlement.py`（两张新表，`create_all` 自动建；明细行**不单独设**软删标记，跟父记录走）、`app/services/order_money.py::line_receivable`（复用，不另算）、Android `ui/shipper/ShipperLedger*.kt` + `ai/AiWriteShipperLedger*.kt` | ⛔ **这是货主自己那一本账，不是公司账**：写它**只**落 `shipper_settlements(+_lines)`，一个字节都不写 `orders.paid` / `cash_flows` / `ledgers` —— 写了就是"公司账上凭空多一笔已收、钱还在他口袋里"，而两边都不报错（`tests/test_shipper_settlement.py::test_settlement_does_not_touch_dispatcher_books` 钉着）。<br>⚠️ 只有**批发商货主**（`users.is_member=1`）能写（普通货主 403，话术要说清"你给自己下单，没有第二个债务人"）；读也只有货主自己（派单员/司机 403，角色门走 `Depends(require_roles(SHIPPER))` —— 写成体内判断的话 AI 读能力目录会把它标成"大家都能读"，模型就会答应一件必然 403 的事）。<br>⚠️ 撤销 = **软删**（父记录的 `is_deleted` 是唯一判据）。<br>⚠️ 列表窗口按**订单的送达日**（不是核销时间）：否则"上个月送的单、今天收到钱"会从本月账面上消失，那一单看起来又变成没核销 |
| **订单退货 · 与退货申请（两者是两件事、两个人）** | `app/services/order_return.py::return_order`（**执行的唯一入口**：红冲 + 回补 + 退现 + 状态，五样一起动；⛔ 金额只算一处 `_line_amount`：本行货值落四位、到分只在「整次退货」这一层做一次 —— **本次退货金额 = 账本红冲 = 退现，是同一个数**（2026-09-21 修掉原来两边各算一遍、四位单价下差 1 分的缺陷））、`app/api/v1/orders.py::return_order_endpoint`（`POST /orders/{id}/return`，权限 `ORDER_RETURN` = **只有派单员**）<br>**申请侧**（2026-09-21）：`app/api/v1/return_requests.py`（6 个端点）、`app/services/order_return_request.py`（submit/withdraw/reject/fulfill ＋ `close_by_direct_return`：直连退货时把那张申请转 `CLOSED`） | `app/models/order_return_request.py`（两张新表，`create_all` 自动建；明细行跟父记录走）、`app/schemas/return_request.py`、`app/services/inventory_service.py::restock_returned`、`android/.../core/ReturnRules.kt`（可退上限的客户端镜像）、Android `ui/shipper/ShipperReturnRequestsScreen.kt` + `ui/dispatcher/DispatcherReturnRequestsScreen.kt`、AI `ai/AiWriteReturnRequest*.kt`、消息中心**直达**那一套 `ui/messages/NoticeRouting.kt`（唯一一处按 `type` + 角色路由）＋ `ui/common/ReturnRequestFocus.kt` | ⛔ **申请阶段什么都不动**（用户原话：「批发商只是一个申请，**派单员才是实际性的操作**。派单员进行完了之后，整个才进行库存才会发生一个改变和变动」）：`submit` 只写 `order_return_requests(+_lines)` + 一条审计日志，**不碰** `ledgers` / `inventory_movements` / `order_products.returned_quantity` / `orders.status` / `cash_flows`。理由写在 `rbac.py`：货主按一下就能改自己的应收和公司库存，那份账就没有第二个人核对了。<br>⚠️ **权限点必须两个**：`ORDER_RETURN_REQUEST`（货主，**只申请**）与 `ORDER_RETURN`（派单员，真退）。合成一个 = 把上面那条线作废。⚠️ `_check_order_return.py` 搜货主矩阵时用的是 `ORDER_RETURN\b`：**裸子串**会把 `ORDER_RETURN_REQUEST` 当成"货主也有退货权"（`rbac.py` 里那段注释因此刻意不写那个权限点的全名）。<br>⚠️ **数量锁死**（2026-09-21 用户拍板）：`fulfill` 的数量只能来自申请单，端点**没有数量参数**；申请之后余量变小 → 拒绝并让派单员驳回（⛔ 不许"按现在还剩多少悄悄退一点"，也⛔ 不许把申请标成已办而实际没退成 —— 那是"货主以为退了、库存没回来"）。<br>⚠️ 一张单**同时只允许一条待处理申请**（`pending_for_order` + 提交时锁订单行）；撤回不是删除（状态变 `withdrawn`，记录留着）。<br>⚠️ 退货红冲行按 `(order_product_id, source=RETURN)` **累加**（多次退货同一行），带损的那几件不许退。<br>⚠️ **直连退货（订单管理那条路）现在是允许的**（2026-09-21 用户拍板，规则从"400 拦住"反了过来）：但退完必须调 `order_return_request.close_by_direct_return(...)` 把那张待处理申请转 `CLOSED`，且必须在该端点的 `db.commit()` **之前** —— 漏了它或顺序反了，申请就还停在 `pending`，再点「办理」时余量够、校验全过 → **同一批货被退两遍**（红线 `_check_return_request.py` §7 与它的反向验证 ⑥/⑥b 钉着）。<br>⚠️ **消息中心点通知要能直达**（同一天用户要求：「其实本来就要做到直达的」）：`ui/messages/NoticeRouting.kt` 是**唯一一处**按通知 `type` + 当前角色算路由的地方（派单员收 `order.return_request` → 派单端待办页；货主收 `.done`/`.rejected`/`.closed` → 货主端「我的退货申请」，带 `?focus=<申请单号>`）；两页的 VM 用 `focusRequestId` 把那条**排到最前 + 打标记 + 滚到它**，找不到时如实显示全部并加一行说明（⛔ 不白屏、不假装定位到了）|
| **输入校验与报错**（改任何入参 schema 前先看） | `app/schemas/text.py`（**文本长度上限的唯一定义处**）、`app/schemas/money.py`（金额容量）、`app/schemas/geo.py`（坐标范围）、`app/core/validation_errors.py`（**422 的中文说明**） | `_tools/qa/_audit_text_fields.py`、`_tools/qa/_audit_money_fields.py` | ⚠️ 上限要与**列宽**对齐（本地 SQLite 照收超长、生产 MySQL `Data too long`）；两层审计工具会逐字段比对。<br>⚠️ 业务范围校验写在**中文 validator** 里，不要写成 `ge/le`（那会变成英文 422 结构体，见 §57.2b） |<br>⛔ **这个处理器自己不许 500**（2026-09-19 外部完整检查 S5）：`NaN` / `Infinity` 是 `json.loads` **默认接受**的字面量（不合法，但 Python 收），它会顺着 `input` 一路走到 `JSONResponse`，而 Starlette 的 `render` 是 `json.dumps(..., allow_nan=False)` → **兜底自己抛异常、用户拿到 500**（实测 `{"quantity": NaN}` → 500；任何带数值字段的写端点都中招）。收口只有一处：`validation_errors._json_safe()`（非有限浮点降级成字符串、递归进 list/dict；**有限浮点原样保留**——否则排障时看不出客户端发的是数字还是字符串）
| **登录 / 账号创建** | `app/api/v1/auth.py`（只有 login/token）、`app/api/v1/users.py`（派单员建号，`USER_MANAGE`） | `app/services/auth_service.py` | ⚠️ **没有自助注册**：`POST /auth/register`、`POST /auth/sms/send`、`services/sms_code.py` 已于 2026-09-18 整体删除（旧客户端会拿到 404）。红线 §28 钉着这条 |
| **司机运费结算** | `app/api/v1/freight_settlement.py`、`app/api/v1/driver_bills.py` | `app/models/driver_bill.py` | 按 `driver_billing_mode` 分 **PIECE / SALARY 两套逻辑**。<br>⚠️ `freight_settlement.py` **自己聚合、不导入 `stats_service`**——`stats_service` 的真实导入者是 `api/v1/stats.py`、`api/v1/reports.py` L443、`services/stats_export.py`。<br>⚠️ `driver_bills` 上有**唯一索引** `uq_driver_bills_order_type(order_id, bill_type)`（v3.41 补，迁移在 `schema_bootstrap.py`）——同一张单插第二条 PIECE 账单会被**数据库**拒绝。<br>⚠️ **月薪单这个索引挡不住**（`order_id` 是 NULL，NULL 在唯一索引里互不相等）→ 并发生成两张月薪单、结算单把两行一起 sum（¥4500 → ¥9000），而确认时的"金额与合计一致"校验**会通过**。所以生成前必须 `with_for_update()` 锁住司机行（2026-09-19 审计；红线 §25b 钉着）。<br>⚠️ **已软删订单的 OPEN 账单不许被结算单收走**（`create_settlement` 必须 `outerjoin(Order)` 排除 `deleted_at`，与运费结算页/报表同口径）——本机实测有"先删单、后付款"的既成事实（2026-09-19 审计） |
| **司机结算单** | `app/api/v1/driver_settlements.py` | `app/models/driver_settlement.py`、`app/services/accounting_service.py::create/confirm/pay/cancel_settlement` | 状态 DRAFT→CONFIRMED→PAID，见 `enums.py`。<br>⚠️ **付款前会再复核一次明细**（还在不在/状态对不对/合计是否相等）：确认与付款是两次点击，中间明细可能被别的路径删掉，钱付出去撤不回来。<br>⚠️ **confirm / cancel / pay 三处都是"条件 UPDATE 占位"**（`UPDATE ... WHERE status=旧状态` + rowcount 校验 + 中文原因）：这三个跃迁在 MySQL 上是并发的，无条件赋值会让 `confirm × cancel` 写成「结算单 CANCELLED + 明细已 SETTLED」（那批账单既不 OPEN 也不可付 → 司机的钱永远结不掉）。红线 §25b 逐条钉着。<br>⚠️ **已确认的结算单目前没有出口**（不能退回草稿/作废）——见台账「待拍板」第 9 条 |
| **报表中心** | `app/api/v1/reports.py`、`app/services/stats_service.py` | `app/services/stats_export.py` | **毛利必须带覆盖率说明**（只算算得出成本的行），否则毛利虚高。<br>⚠️ **毛利的收入侧与成本侧必须是同一批行**：`cost_covered_amount − cost_total`（营业纵览 / 商品页 / 导出表头 / 导出行 / App 全部读它）。曾经三处三个数：App 商品页 11,071 / 正确 10,789 / 导出逐行合计 72,177.75（2026-09-19 审计）；逐行算不出成本时印「—」并说明"不进毛利"。<br>**⛔ 成本口径 = `app/services/cost_basis.py`（唯一实现，2026-09-19 用户要求）**：**入库流水的加权平均进货价**，三级兜底 = 期间均价 → 累计均价 → 下单快照。用户原话：「不能这么算啊，这么算的话毛利率会偏低」——旧口径（订单行 `cost_price_snapshot` = 下单那一刻的最新进货价）在进货价上涨时会把旧库存的成本按新高价算。⛔ **货损金额仍然走快照**（送达时就已按它写进开销账与现金流水，是已入账的历史金额）；出参必须带 `cost_avg_lines` / `cost_snapshot_lines`，界面要同时报两个数。红线 `_tools/qa/_check_cost_basis.py`（8 种注入反向验证 `_reverse_verify_cost_basis.py`）、回归测试 `tests/test_report_cost_basis.py`。<br>⚠️ 绩效/导出的「计费方式」走 `driver_pay.snapshot_mode`（挂了规则看规则），不许读 `users.billing_mode`；**也不许从窗口内的订单快照里随便取一个**（同一窗口可能两种都有，取任意一个是掷骰子） |
| **货损记账** | `app/services/accounting_service.py`（546 行） | `order_flow.py::complete_delivery` | 货损在**送达时**结算 |
| **挂账单位** | `app/api/v1/arrears.py` | `app/models/arrears.py` | — |
| **现金流水** | `app/api/v1/cash_flows.py` | `app/models/cash_flow.py`、`app/models/enums.py::CashFlowBizType` | 收支类型全在 `enums.py` |
| **费用（开销管理）** | `app/api/v1/expenses.py`、`app/api/v1/expense_categories.py`（分类名册）、`app/services/accounting_service.py::create_expense` | `app/models/expense.py`、`app/models/expense_category.py` | **2026-09-20 用户第六轮重写**：①**新增开销 = 单独一页**（`Routes.EXPENSE_CREATE`，「就相当于新增订单一样」）；②记录区**照商品管理**：左边分类栏、右边该分类的记录，分类是**可维护名册**（`/expense-categories`：建/改名级联/删除有挂账拒绝/排序整份/`reorder`，排序的「整份」判据走共用的 `app/services/category_order.py`）；③**时间 = 右上角时间药丸**（复用 `ui/common/Components.kt::DatePresetPill`，默认窗口走 `DatePresets.AUTO_LADDER` 退档）；④**卡片按分类决定突出哪一项**（分类的 `link_kind` = vehicle/driver/order/none，用户：「不能一刀切」），订单来源在**详情**里看（「点击详情……查看这个订单到底是哪里来的」）。<br>⛔ `expenses.category` 从枚举改成**自由字符串 String(32)**（枚举认不出的名字会让 `GET /expenses` 整个 500）；迁移把老英文键翻成中文名并把在用分类回填进名册（`EXPENSE_CATEGORY_RENAME` / `EXPENSE_CATEGORY_LINK`）。<br>⚠️ 现金流水口径映射（`create_expense` 里那张 `biz_map`）**中文名与老英文键都认**、认不出落 `EXPENSE_OTHER` —— 用户新加一个分类不该让这笔开销存不进去。<br>红线 `_tools/qa/_check_expense_page.py`（38 项）+ 反向验证 `_reverse_verify_expense_page.py`（13 条注入）。 |

### 1.3 商品、定价、库存

| 功能 | 核心文件 | 附带文件 | 注意 |
|---|---|---|---|
| **商品（建/改/软删/图片）** | `app/api/v1/products.py` | `app/models/product.py`、`app/schemas/product.py` | ⚠️ 进价 `cost_price` **按角色裁剪**（只有 `product:manage` 拿到真值，其余 `null`）——判据只在 `product_out()` 一处，列表与详情同一个口径。删除是**软删**（可 `POST /{id}/restore`），下架请用 `PATCH is_active=false`。<br>⛔ `tier_prices`（多档批发价）**2026-09-19 已废弃**：它看着像批发价、下单却一个字节都不照它走（下单只认 `price_rules` 的专属价或商品默认售价），所以入参/出参字段与批量调价的 `mode="tier"` 全删；**列与历史数据保留在库里、不写迁移**——别去 drop 它，也别按它写新逻辑 |
| **批发商专属定价** | `app/api/v1/price_rules.py` | `app/models/product.py`（**L49** `class PriceRule`）、`app/api/v1/users.py`（`is_member`） | 批发商 = `users.is_member`；专属价存 `price_rules` 表。<br>`GET /price-rules` 的筛选是**先锁角色、再叠加**：货主先被锁成只看自己的（所以带**别人的** `shipper_id` 得到**空列表**，不是别人的价——下单页正是拿这份价报价的），再叠加 `shipper_id`（某批发商有哪些价）与 `product_id`（某商品各家批发商什么价，App「按商品看各批发商价」页用它；两个条件是并列 AND）。<br>⛔ 批量调价的 `mode` 只有 `fixed` / `percent` / `adjust` 三档：原第四档 `tier`（取商品自身"批发价第 N 档"）随 `tier_prices` 一起删了，传 `tier` 由 schema 直接 **422** 拒掉 |
| **商品分类 category**（下单页左侧分组） | pp/models/product.py（category）、pp/schemas/product.py、Android ui/dispatcher/ProductsScreen.kt + ProductsViewModel.kt（draftCategory） | pp/api/v1/products.py（创建/更新都 strip()）、pp/core/schema_bootstrap.py（补列） | 空串 = **未分类**（老数据全在这一档）。分类清单**从商品算出来**（`ProductPicker.categoryTabsOf`），不是手写枚举；顺序 = 全部 → 各分类按名册 `sort_order` → 名册外的按商品数倒序 → 未分类（**全是未分类时它不出现**，否则和「全部」内容一样）。<br>⚠️ **判据只有一份实现**：`categoryTabsOf(names, ordered)` 是本体，`categoryTabs(products, ordered)` / `categoryOf(p)`（商品→档位）与 `categoryNameOf(raw)`（分类名→档位）都是它的入口 —— 商品管理、库存管理、选品页三处共用。各写一份就会出现"同一件商品在这页属于日化、在那页属于未分类"（红线 §30 里专门钉着"不许出现第二份实现"） |
| **成本价历史（生效区间）** | `app/models/product.py::ProductCostHistory`、**`app/services/cost_history.py`（唯一写入口）**、`app/api/v1/products.py::product_cost_history` | `app/core/schema_bootstrap.py`（建表后**回填**）、`app/services/data_retention.py`（3 年清理）、`app/schemas/product.py`、**Android** `ProductsScreen.kt::CostHistoryDialog`、`ui/theme/Color.kt::QuickPriceGreen` | 用户 2026-09-19：「成本价做一个保留…**保留它存在的时间**，从什么时候开始变、从什么时候结束，**精确到小时和分钟**」→ 每行是**一段区间**（不是"改动日志"），`effective_to IS NULL` 那行 = 当前生效价。<br>⛔ **`products.cost_price` 只有三个写入口，全部必须走 `record_cost`**：建商品（CREATE）／进货带价（PURCHASE，带 `movement_id`）／编辑里改（MANUAL）。绕过它直接赋值 = 价格变了、区间表没变，**"这段时间的成本价是多少"从此对不上账而界面上看不出来** —— 红线 `_tools/qa/_check_cost_history.py`。<br>⚠️ **价没变就不写**（否则每次编辑商品都塞一段零长度区间，"这个价用了多久"就查不出来）；⚠️ 区间**首尾相接**（旧行 `effective_to` == 新行 `effective_from`，半开区间）；⚠️ 保留期**跟着账本走**（用户原话），清理判据用 `effective_to` 而**不是** `effective_from` —— 用后者会把"三年前定的价、现在还在用"那一行删掉。<br>⚠️ 路由 `/products/cost-history` **必须声明在 `/{product_id}` 之前**（Starlette 按声明顺序匹配，反了会返回 422 而不是 404）；商品用**查询参数**而不是路径参数，因为 **AI 看不到任何内部编号**，带 `{}` 的端点对它是死的（`_gen_ai_read_catalog.py` 按"路径里有没有 `{}`"排除）。<br>⛔ 权限 `PRODUCT_MANAGE`：货主/司机连商品详情里的 `cost_price` 都拿不到。回归测试 `tests/test_product_cost_history.py`（8 例） |
| **库存 / 库存流水** | `app/api/v1/inventory.py` | `app/services/inventory_service.py`、`app/models/inventory.py`、`app/schemas/inventory.py`、**Android** `ui/dispatcher/InventoryScreen.kt` + `InventoryViewModel.kt` | `products.stock` 只是创建时初值，**后续走流水**。<br>⚠️ **缺货判据写在 `UPDATE ... WHERE` 里**（`stock + change >= 0`），不许"先读再判再写回绝对值"：那是 TOCTOU，库存 10、两人同时出库 8 会都放行（一共出库 16、账面 2）；"写回绝对值"还会把并发的另一笔加减整段盖掉（lost update）。<br>⚠️ **「在途占用」必须 `join orders` 并排除软删单**，否则已物理清理订单的 RESERVED 流水会永久被算成在途（保留任务现在会连库存流水一起删）。<br>⚠️ 计数列只能由数据库自增（`stock = stock + delta`）——红线 `_tools/qa/_check_counter_updates.py`、回归测试 `tests/test_audit_round16_counters.py`<br>**进货价（2026-09-19 用户要求）**：入库可以带 `unit_cost`，它做**两件**事（同一事务，缺一不可）：① **记在流水上**（`inventory_movements.unit_cost`）—— 这是"这批货多少钱"的一手数据，**毛利的加权平均进货价就从它算**；② 把 `products.cost_price` 更新成它（走 `record_cost`，顺带开一段成本价区间）。⛔ 只做 ② 就等于把成本信息丢了（本轮之前就是这么写的，毛利因此只能用"最新一次进货价"）。出库存价 **400**（不是"收下但静默无效"）；成本旧价→新价进操作日志。⛔ 这一列**老库要有一条 ALTER 迁移**（`core/schema_bootstrap.py`）：本机新库靠 `create_all` 自带，测不出老库缺列。回归测试 `tests/test_inventory_unit_cost.py` + `tests/test_report_cost_basis.py`。<br>**页面版式（2026-09-19 用户要求）**：库存管理 = **左边分类、右边商品 + 按名称搜索**（与商品管理同版式；用户原话：「库存管理也是跟商品管理一样的…并且可以通过搜索名称来搜索商品，来盘查实时库存是怎样的」）。⛔ 分类导航条与判据**复用** `ui/common/ProductPicker.kt`（`CategoryRail` / `categoryTabsOf` / `categoryNameOf`），顺序取 `GET /product-categories`；分类值随 `/inventory/summary` 的行下发（`InventorySummaryItemDto.category`）—— 那一行里没有它的话，安卓侧就只能再拉一次全量商品列表自己 join，商品一旦不在列表里（刚下架）那一行会静默变成「未分类」。⛔ 出入库弹窗吃的是 `/inventory/summary` 的那一行，**不是** `ProductDto` —— 原来是 `products.firstOrNull { it.id == ... }`，商品不在列表里时按钮**点了什么都不发生**（静默） |
| **货主 / 批发商 / 客户管理** | `app/api/v1/customers.py`、`app/api/v1/users.py` | `app/models/customer.py`、`app/models/user.py` | 改工资/计费/车型字段，非派单员会 **403**。<br>⚠️ **合并（`POST /customers/merge`）是不可逆的**（customers 表连 `is_deleted` 都没有）：把引用搬到 keep 上、再**物理删除**被并档案。三条闸门：① `keep_id` 不许同时出现在 `merge_ids`（否则两个档案一起被删）；② 两个**不同 `user_id`** 的注册客户不许合并（订单归属绑在账号上，`orders.shipper_id` 这一列没有搬 → 那批历史应收会永久失去核销入口）；③ 临时货主（无账号）的名字在库里存**两份**（`orders.temp_shipper_name` 与 `ledgers.temp_shipper_name`）——合并时**两份一起改**，只改账本那一份的话 `ledger_sync` 下一次同步会拿订单上的旧名字覆盖回去，改名被静默还原（R14-4，回归测试 `test_merge_renames_both_copies_and_survives_a_resync`）。**没有重命名端点** |
| **车辆 / 运费模板** | `app/api/v1/vehicles.py`、`app/api/v1/freight_templates.py`（**2026-09-21 起还带 `/quote` 报价**）| `app/models/vehicle.py`、`app/models/freight_template.py` | 车型校验：`freight_templates.py::_validate_vehicle`（运费模板）、`vehicles.py::_clean_type`（车辆本身）。<br>**两条写路径、一份实现**（v3.44 修完）：`PATCH /vehicles/{id}` 改车牌/车型/启停用（**车牌查重了，且排除自己**），`POST /vehicles/{id}/driver` 绑/解绑司机（`driver_id` **缺省或 null 都 = 解绑**）——两者共用 `_apply_driver`，所以校验与错误文案不会分叉。想看"只改车型也带上同一个车牌"这类自己撞自己的坑，看车辆那一节的红线。<br>⚠️ 为什么解绑必须走专用入口：安卓 `Json { explicitNulls = false }` 会把 `Long? = null` **整个键丢掉**，客户端**发不出**"显式 null"——所以"缺省 = 解绑"被写进契约（与 `POST /driver-billing-rules/attach` 同形）。<br>⚠️ `users.vehicle_type`（"他能开什么车"，决定计费口径）与 `vehicles.vehicle_type`（"这是辆什么车"）**是两件事**：绑车**不会**改司机的车型，界面上只把不一致**说出来**——自动改一次就等于悄悄改了他以后怎么算钱。<br>**Android**：`ui/dispatcher/VehicleManageScreen.kt`（列表/编辑/绑司机/停用）、`UsersManageScreen.kt` 的车辆行 + `VehiclePickerSheet`（司机视角）、`data/remote/api/Apis.kt::updateVehicle/setVehicleDriver`、`dto/VehicleUpdateRequest` / `VehicleDriverSetRequest`<br>**AI**：`ai/AiWriteBasicData.kt`（`vehicle.create` / `vehicle.update` / **`vehicle.set_driver`**：不填司机 = 解绑，卡片必须写"现在归谁"）、`ai/AiResources.kt`（撤回走 **`update` 那种"把 payload 里的键写回旧值"**，配 `CrudSpec.alwaysIncludeTargets` 保证解绑时 payload 里也带 `driver_id=null`）<br>⚠️⚠️ **资源表里的"逆操作是谁"是个会静默失效的声明**：`paired(X, AiInverse(Y,…))` 的 `Y` **绝不能等于 `X`**（`AiRevert.pairedPlan` 第一行就排除自逆 → 撤回方案永远造不出来，而 `canRevert()` 照样返回 true，卡片敢承诺却兑现不了）。红线 §2e-③f 钉着这一条 |

### 1.4 地址、消息、实时

| 功能 | 核心文件 | 附带文件 | 注意 |
|---|---|---|---|
| **地址与联系人**（三列表结构） | `app/api/v1/shipper.py`（405 行） | `app/models/shipper.py`、`app/schemas/` 对应 schema | 表归属用 **`user.id`**（字段名 `shipper_id` 是历史包袱）；多图走 `image_urls` JSON + `image_url` 首图兼容。<br>⚠️ **`services/shipper_contact_service.py` 不属于这里**——`shipper.py` 不导入它，唯一导入者是**下单链路** `api/v1/orders.py` L70（`upsert_boss_contact`） |
| **消息中心 / 通知** | `app/api/v1/notifications.py`、`app/services/message_center.py`（416 行）；Android `ui/messages/MessagesViewModel.kt` + `ui/messages/MessagesScreen.kt` | `app/models/notification.py`、`app/schemas/notification.py` | 30 天保留期是**惰性清理**，没有定时任务。**列表/删除/未读三处口径必须同源**：默认都是"自己的"，派单员要看别人要显式传 `recipient_id`（v3.32 修掉"清空后重启消息复活"）。客户端改动一律**以服务器回包为准重拉**，不做乐观置空。<br>⚠️ **列表分页是"截断有回报 + 有入口"两条腿**（2026-09-19 审计 R14-8）：`limit`（缺省 200、上限 5000 与 `/orders` 同档）+ `before_id` 游标 + 响应头 `X-Truncated`/`X-Result-Limit`（**与 `GET /orders` 同一个形状**）；安卓侧由 `Response<...>` 读响应头 → `NotificationPage.hasMore` → `MessagesViewModel.loadMore()` → 界面「加载更早的消息」按 `vm.hasMore` 门控。四条接线由 `_tools/qa/_check_pagination_wiring.py` 逐段钉着（少一条就红）。<br>⚠️ **`days` 的窗口起点走 `business_time.utc_now_naive()`**：`created_at` 存的是 UTC，用 `datetime.now()`（进程本地时间）去比会静默少 8 小时。<br>⚠️ **`batch-delete` 的 `ids: []` 会被 400 拒掉**（原来是"参数为空 = 删光该账号全部消息"） |
| **待派池 / 站内信实时推送**（**四层，别只看一层**） | ① `app/core/socket_io.py`（server）→ ② `app/services/message_push.py`（`emit_to_user`）→ ③ **`app/services/message_center.py`（文案/收件人/payload 与未读数，真正的业务逻辑）** → ④ `app/services/push_events.py`（订单事件封装） | `app/api/v1/orders.py` | 只改 `socket_io.py` 或 `push_events.py` 会漏掉真正决定"发给谁、发什么"的 `message_center.py`。<br>⛔ **撤销会话必须同时断开长连接**（2026-09-19 外部完整检查 C-3）：`tv`（令牌版本）**只在握手校验一次**，所以登出/改密/停用之后旧令牌打 HTTP 全 401、**但那条已经建起来的连接继续收推送**（站内信正文、单号、账本变动）。唯一入口是 `app/services/auth_service.py::revoke_tokens_and_sockets`（作废令牌 + 排一个后台断开任务，三个撤销点都调它，**不可能只做一半**）；它调 `app/core/socket_io.py::revoke_user_sockets`：先 `emit("session_revoked")`（经 Redis 适配器**跨 worker** 送达，客户端收到就自己走清会话那条链），再断**本进程**里的连接（兜住还没升级、不认识那个事件的旧客户端）。⚠️ 残留：旧客户端若恰好连在**另一个** worker 上，要等它下次重连（握手会被 `tv` 判据拒掉）才真正断开 |
| **图片上传 / 压缩** | `app/services/image_archive.py`（`compress_image` 原地压缩 / `archive_images_older_than` / `purge_orphan_images` 按**引用**清无人引用的图 / `purge_orphan_compressed` 清中间文件） | `app/api/v1/orders.py`（L563 `upload_order_address_image` 地址图、L618 `upload_delivery_photos` 送达图）、`app/api/v1/shipper.py::upload_location_image`（地点图 → `uploads/locations/{uuid}.{ext}`） | 原图 1 年后**原地**压缩（Q90 / 长边 1920，**文件名与扩展名都不变**，所以库里的 URL 一直有效）。<br>⛔ **归档前必须先用 header 里的尺寸判像素数**（`MAX_ARCHIVE_PIXELS = 6000 万`）：解码峰值 ≈ 宽×高×通道数，而"尺寸"是**客户端说多大就多大**——实测不到 1MB 的图声明 8100 万像素就能吃到 260MB+，且**每天都重试**（Pillow 只在自己的 8947 万阈值**告警不拦**，要到 1.79 亿才抛）。超限的唯一正确动作是"保留原图 + 记一条 warning"，**不许**先解码再判断。<br>⛔ `purge_orphan_images` 会**删文件**，它删不删的唯一判据是 `REFERENCED_IMAGE_COLUMNS`（哪些表的哪些列还引用着这个 URL，**软删行也算**）——漏一列 = 删掉还在用的图且不可恢复。所以那条清单有专门的红线：`_tools/qa/_check_image_refs.py`（**自己扫** `app/models/` 里所有 url 列，清单过期时先报错）。`delivery/` 刻意**不在**扫描范围（送达凭证的寿命绑在订单上，由 `delete_orders_by_ids` 按单清） |
| **共享地点库 / 订单补导航**（司机到场标坐标 → 全库共用） | **后端**：`app/services/place_service.py`（**合并判据唯一实现**：`MERGE_METERS=1.0` 坐标、`SAME_NAME_METERS=30.0` 同名；`AUTO_ADD_AFTER=2` 常用地点阈值）、`app/api/v1/places.py`（列表/新增/`POST /places/{id}/use`）、`app/api/v1/orders.py::fill_order_navigation`、`app/models/place.py`（`Place` + `PlaceUserUsage`）<br>**Android**：`ui/order/OrderDetailScreen.kt`（`NavigationBlock` + `NavigationFillDialog`）、`ui/shipper/OrderCreateScreen.kt`（`AddressPickerSheet` 两栏：左栏 = 三段 + 自定义分组 + 底部「管理分组」，右栏 = 内容）、`ui/common/AmapPicker.kt` | `app/models/order.py`（`nav_source`）、`app/models/enums.py`（`ORDER_NAVIGATION_FILL`/`PLACE_AUTO_ADDED`）、`app/schemas/place.py`、`_tools/qa/_probe_place_merge.py` | ⚠️ **一张表不按人分区**（用户明确要"共同的库，相同位置直接拉过来"），三种角色都能查；<br>⚠️ **只补不改**：订单已有坐标一律 400（错坐标比没坐标更危险）；<br>⚠️ 一次补录写**三处**：这一单 + 货主自己的地点库 + 全库共享库；<br>⚠️ 合并判据**只有一处**；<br>⚠️ **常用地点按 (人, 地点) 计数**（不是全库 `use_count` —— 那是"大家都去过这儿"，用它会把热闹地点涌进所有人列表），到 `AUTO_ADD_AFTER` 自动进他自己的「我的地点」并留痕；<br>⚠️ **位置照片跟地点一起进「我的地点」**（2026-09-20 用户：「照片跟地点是一样是自动保存在库里的」）：唯一实现 `place_service.attach_order_photo`（在 `POST /orders/{}/address-image` 里调，与 `remember_order_address` **同一批 owner、同一套判据** —— 两处各判一次会出现"地点进了这条、照片进了那条"）。**上传权限同时放行了这单的司机**（原来是派单员 + 货主两角色；而到过现场的人正是唯一拍得出"这个门口长什么样"的人）。订单详情页的 `PlacePhotoStrip` 是唯一展示口（点缩略图看大图）；<br>⚠️ **AI 可以补导航，但坐标只能引用共享库里已有的点**（`orders.fill_nav` → `AiWriteOrderHandlers.kt::FillNavigationHandler` → `ds.placeById`）。这条以前是"AI 不做补导航"（坐标类永久排除）——**原则没变**：模型说的是"用哪一个地点"，坐标由 App 从库里取，模型全程碰不到经纬度（红线 `_check_ai_guardrails.py` 锚着 `put("address_lat", point.addressLat...)`，反向验证注入的正是"改成用模型给的坐标"）；<br>⚠️ **下单选的地点自动进「我的地点」**（2026-09-20 用户：「只要用户下单他会选择地点，这个时候，我们就自动地把它添加到地点库当中」）：唯一实现 `place_service.remember_order_address`（`orders.py::create_order` 里调）。**只进「我的地点」，绝不动全库共享那张表**（那张表有三个明确入口，见下面「共享地点库」一行）；代理下单**下单人 + 货主两边都记**（库按登录人隔离，只记一边另一边下次还得重新找这个地址）；判据：有坐标走 `ensure_shipper_location`（1 米 / 同名 30 米），只有文字走 `_find_known_location`（**地址原文一模一样**，或名字一样且那一条还没坐标 —— 不做模糊匹配）；真的新建了才写 `PLACE_AUTO_ADDED`（与"常用地点自动进库"**同一个动作码**，这样"我的地点怎么多出一条"只有一个地方要查）；<br>⚠️ **先文字、后坐标 = 把那条补全**（`ensure_shipper_location` 2026-09-20 新增的一支）：下单时只写了文字地址、司机到场才补坐标是最常见的顺序，少这一支的后果是同一个地点在货主库里趴着**两条同名记录**（一条有坐标一条没有），列表上分不出哪条是哪条；<br>⚠️ **两条自动进库路径的阈值刻意不同**：在列表里**点选**要到第 2 次（`AUTO_ADD_AFTER`），**下过单**的地址第 1 次就进 —— "点了一下"和"真把货送过去了"是强弱不同的信号。`note_place_use` 报回的 `auto_added` 是**真的新建了**才为 true（以前报的是"阈值到了"，而库里可能早就有那一条 → 界面会说一句"已加进你的「我的地点」"而列表纹丝不动）；<br>⚠️ AI **不做**这两个写端点（模型给不出经纬度）；<br>⛔ **`(0,0)` 不是坐标，是"没有定位"的占位值**（2026-09-19 报告 P1-13）：高德定位失败回的就是 `(0,0)`（不是 null），一条脏点会被别人的单当成导航点。（2026-09-19 起共享库能改、能软删了，但"指望有人去清理历史脏点"不是个方案。）判据只有一处 —— 安卓 `core/SunLocation.isPlausible`（阈值：`abs(lat)<0.01 && abs(lng)<0.01`，单独一个 0 是合法坐标）；三个入口都要过它：`AmapPicker` 的确认口（它初始坐标就是 `initialLat ?: 0.0`）、`ai/AiGeocode.kt`（解析成 `(0,0)` 时按"没解析出来"返回 null）、后端 `app/schemas/place.py::PlaceCreate._coords_required`（闸放这里而不是 `GeoInput`：地址/订单那几张表有历史脏行，拦在它们入口会让"改个电话"也被迫重选坐标） |
| **商品分类名册**（下单页左侧那一列叫什么、按什么顺序） | **后端**：`app/models/product_category.py`、`app/api/v1/product_categories.py`（增/改/删/`reorder` + `ensure_category`）、`app/schemas/product_category.py`<br>**Android**：`ui/dispatcher/ProductCategoriesScreen.kt` + `ProductCategoriesViewModel.kt`（入口在**商品管理页右上角**；排序 = **长按拖动** 或 **直接改左边的序号**）、`ui/common/ProductPicker.kt::categoryTabs`、`ui/dispatcher/ProductsScreen.kt`（编辑页从名册**下拉选**分类，也能就地新建） | `app/core/schema_bootstrap.py`（建表 + **存量回填**：把已在用的分类名收进名册、按商品数排序）、`app/api/v1/products.py`（新建/改商品时自动补名册）、单测 `android/app/src/test/java/com/tapmoay/sorders/ui/dispatcher/ProductCategoriesOrderTest.kt`（排序的两段纯逻辑 `moveCategoryTo` / `dragSteps`） | ⚠️ **改名必须级联**（同一事务里 `UPDATE products SET category=新名`）——不级联的话商品全变未分类且**不报错**；<br>⚠️ **删除时有商品挂着 → 拒绝**并说明有几个（不"顺手把商品改成未分类"）；<br>⚠️ `reorder` 必须**整份**提交（只传一部分会被拒并点名少了哪些）——判据只有一份：`app/services/category_order.py::ordered_ids`（**商品/地点/开销/运费四个名册共用**，2026-09-21 从四个端点收口）；所以**排序是本地草稿**，改完要点「保存顺序」，拖动/改数字都不会自动提交；<br>⚠️ 名册里没有的分类名**不是错误**，选品页会排到名册后面——**不许因为它不在名册里就把商品藏起来**<br>**排序交互（2026-09-19 用户要求，取代原来的上下移按钮）**：<br> · **长按拖动**：手势挂在"名称区 + `⠿` 手柄"上（**不能挂整行** —— 序号框要留着自己的长按选字、`⋮` 要留着自己的点击）；<br> · **填数字**：左边序号框可改，填几就排到第几（0/越界夹到两端）；数字排序与拖动**共用** `moveCategoryTo` 一处实现；<br> · ⛔ **两个真机才暴露的坑**（都写在代码注释里）：① 列表重排时**必须 `key(c.id)`**，否则被拖动那一行的 `pointerInput` 随节点重建被销毁 → 手势取消 → "拖半天只挪一格"；② 序号框**聚焦要全选**，否则用户点一下（光标在数字前面）再打字会变成 `42` 而被夹到末尾。<br>**AI**：`ai/AiWriteBasicData.kt`（`product_category.create` / `update` / `delete`）、`ai/AiWriteCatalogHandlers.kt`（`product_category.reorder`：**整份顺序**，漏一个就拒绝并把新顺序逐行列在卡上）、`ai/AiResources.kt` + `ai/AiRevert.kt`（改＝写回旧值；**删＝按原名重建一格**——名册没有软删，所以撤回卡上写的是"重建"而不是"行还在"；**重排撤不回来**，要用户把顺序重说一遍）<br>⚠️ 卡片的硬要求：改名卡必须写出**这个分类下有 N 个商品**（改名会级联改过去） |
| **商品可见白名单**（某个货主/批发商只能看到勾选的商品） | **后端**：`app/schemas/product_visibility.py`（**判据唯一处**：`visible_product_ids` / `product_visible_to` / `replace_visibility`）、`app/models/product_visibility.py`、`app/api/v1/users.py`（`GET/PUT /users/{id}/product-visibility`） | `app/models/user.py`（`product_scope` 开关）、`app/api/v1/products.py`（列表过滤 + 详情按 404 回）、`app/api/v1/orders.py`（**下单也拦**）、Android `ui/dispatcher/UsersManageScreen.kt::ProductVisibilityBlock` + `UsersManageViewModel.kt` | ⚠️ **默认 `all`（不限制）**，开关在 `users.product_scope` 上：空白名单 + `all` = 不限制；空白名单 + `custom` = **真的什么都看不到**（两者语义绝不相同，所以不受限用 `None` 表示而不是空集合）；<br>⚠️ 迁移**必须回填 all** —— 回填成 custom 会让所有老货主上线即空目录；<br>⚠️ **派单员不受限**（否则改错了没人能改回来）；<br>⚠️ 只在选品页藏起来不够：**列表、详情、下单三处都要拦**，否则"看起来限制了、其实没有"<br>**AI**：`ai/AiWriteCatalogHandlers.kt`（`user.product_visibility`，**HIGH**：本质是授权）——卡片必须写清**改的是谁 / 现在什么模式 / 改成什么模式 / custom 时把商品名逐个列出来**；`custom` 但一个都没勾或商品名对不上**在弹卡之前就拒绝**（后端也会 400，但那时用户已经点过确认了）；改回 `all` 要写明**旧白名单会被清掉**<br>**撤回**：`ai/AiResources.kt`（`product_visibility` 资源）把开关和白名单**整份写回**——白名单是内部编号，所以它是**静默键**（跟着写回但不占卡片一行） |
| **一次性提示条**（snackbar） | `ui/common/Components.kt::OneShotSnackbar`（**先消费、再显示**） | 全 UI 层 20 个调用点 | ⚠️ **不许写"先 `showSnackbar` 再清状态"**：`showSnackbar` 会挂起几秒，用户在这期间切页 → 协程被取消 → 清状态那行永不执行 → **切回来提示条又冒出来**（用户 2026-09-18 报的就是这个）；<br>⚠️ 加载错误与动作错误要**分成两个字段**（`loadError` / `error`），否则提示条一消费，整页「加载失败 + 重试」也一起消失；<br>⚠️ 点一下就弹的那种（`scope.launch { showSnackbar("已复制") }`）本来不会重放，**不用**绕这一层 |
| **收货人与下单人**（订单上的两个联系人：**名称 + 电话**） | **后端**：`app/models/order.py`（`contact_dongjia_name` / `contact_boss_name`，与两个电话列一一对应）、`app/schemas/order.py`（`OrderCreate` 可写 / `OrderUpdate` 可改 / **`OrderOut` 要出**）、`app/api/v1/orders.py`（创建、PATCH、**搜索 `q` 的两条 OR 分支**）、`app/core/schema_bootstrap.py`（旧库补两列）<br>**Android**：`ui/shipper/OrderCreateScreen.kt`（「联系信息」里四个框：收货人名称/电话、下单人名称/电话）、`ui/shipper/OrderCreateViewModel.kt`（`prefillOrderer` + `applyAddress`）、`ui/common/OrderCard.kt`（`contactWho` **唯一一份拼接** + 卡片两行）、`ui/order/OrderDetailScreen.kt`、`ui/dispatcher/DispatcherOrdersScreen.kt`（编辑订单弹窗）、`ai/AiWrite.kt` + `ai/AiWriteOrderHandlers.kt` + `ai/AiResources.kt` | 用户 2026-09-20（对着代理下单页截图）：「再加 2 个信息，**收货人的名称**和**下单人的名称**……这个下单人会**根据自己的账号**来自动填选……卡片的信息会显示：一个是收货人是谁、一个是下单人是谁，**详情也会显示**这 2 个信息，这边的电话号码都会显示出来」。<br>**口径**：dongjia=**收货人**（到现场接货的人）、boss=**下单人**（下这一单的人，代下单时就是派单员）。<br>⚠️ **自动填只有两条来源**：下单人 ← 当前登录账号（`/users/me` 的姓名+电话，货主/批发商/派单员同一套规则）；收货人 ← 选中的那条线路（`shipper_addresses.receiver_name`）。<br>⛔ **不许拿会话里的 `username` 当电话**（老数据/种子里它可能是人名 → 会填一个打不通的号）；取不到账号资料就只填姓名，电话留空。<br>⛔ **名称与电话缺一个就只显示有的那个，两个都空就不画那一行**（老单没记过名字，画一条「收货人：-」是空的）。<br>⛔ 用词只有一套：**收货人 / 下单人**（旧词「东家电话 / 老板电话」已从界面与 AI 目录里清掉）。<br>红线 `_tools/qa/_check_contact_names.py`（40 项，四层接线）+ 反向验证 `_tools/qa/_reverse_verify_contact_names.py`（14 条注入，每条都能让红线点出那一条） |
| **下单页选品（外卖式）** | `ui/common/ProductPicker.kt`（全屏弹层 + `categoryTabs` + `QtyDialog` **只填数量**） | `ui/shipper/OrderCreateScreen.kt`（货主与代理下单**共用**；行编辑弹窗 `LineEditDialog` **既没有单价也没有单位输入**）、`ui/shipper/OrderCreateViewModel.kt`（`mergePickedIntoLines` 纯函数）、单测 `android/app/src/test/java/com/tapmoay/sorders/ui/shipper/ProductPickerTest.kt` | 可在一次里挑多件；同一件商品**累加数量**不新开行；超过 `MAX_ORDER_LINES=10` **整批拒绝**（不做部分成功）<br>⛔ **下单的人不能改单位、也不能改单价**（2026-09-19 用户要求，两条都是"能改的字段就是会被改错的字段"）：<br> · **单位**唯一来源是商品库 `products.unit`（派单员在「商品管理」里设）。用户原话：「那个单位不要出现…货主去下单的时候他是不能去更改单位的不然会出现认知判断错误」→ `QtyDialog` 连参数都不收单位，选品小窗里没有单位选择区、行编辑弹窗里没有单位输入框。<br> · **单价**唯一来源是商品定价（`products.default_unit_price`，批发商走 `price_rules.special_unit_price`）。用户原话：「它这个选的商品页面它是不能改订单价的不然那货主他想改多少就改多少」→ 行编辑弹窗里那个「单价（元）」输入框已删（改错的是**钱**，且不会有任何提示）。弹窗里保留「小计」= 数量 × 单价，钱照样看得见。<br>⛔ **红线**：`_tools/qa/_check_input_rules.py` 里 `NO_PRICE_EDIT_FILES` 会扫 `ProductPicker.kt` / `OrderCreateScreen.kt`，**出现任何标题含「单价/价格/售价/金额」的可编辑输入框就报红**（想展示就换成只读 `Text`）。<br>⚠️ 顺带删掉了一条**从来没被走到过**的死分支：「新增手输的自定义商品行」（`editingLineIndex` 越界即新增）—— `editingLineIndex` 的唯一赋值点是列表行上的编辑按钮，越界不可能发生；而且删掉单价输入之后，真走进去只会产出一行**没有价格**的行（手输行没有商品库给它定价，后端会按空单价收下 = 0 元）。后端仍支持 `product_id` 为空的行（AI 的手输行用得上），只是下单页不再开这个口子 |

### 1.5 跨层不变量（改接口/时间/图片/输入框前先看这几行）

| 功能 | 核心文件 | 附带文件 | 注意 |
|---|---|---|---|
| **输入框的数字与格式约束**（电话只能数字 / 单价至多 4 位小数 / 金额至多 2 位 / 数量只数字） | **App 侧唯一实现**：`core/InputRules.kt`（`mobileInput/mobileError` = 手机号也就是登录账号；`phoneInput/phoneError` = 联系电话，7~12 位、**收座机**；`moneyInput/moneyError` = 金额 2 位小数；`priceInput` = **单价 4 位小数**；`intInput`；`splitPartsInput`）。<br>**后端另一半**：`backend/app/core/phone.py`（同一口径的 Pydantic 校验，`schemas/user.py`、`schemas/order.py` 的两个联系人电话、`schemas/shipper.py`、`schemas/arrears.py`、`schemas/accounting_v2.py::CustomerCreate` 都标它） | 单测 `android/app/src/test/java/com/tapmoay/sorders/core/InputRulesTest.kt`（JVM，不依赖模拟器）；红线 `_tools/qa/_check_input_rules.py`（**自己扫** Android 源码算出全部文本框 → 认得出电话/金额类的必须出现 `InputRules.*`；`EXCLUDED` 例外必须写明理由且不许变成化石；外加**下单链路不许出现价格输入框**）；反向验证 `_tools/qa/_reverse_verify_input_rules.py`（12 条注入） | ⚠️ **不许在调用点手写 `filter { isDigit() }` / `c == '.'`**：那是规则的一份副本，改的时候一定漏掉一个（红线第 3 条报）。<br>⚠️ **单价 4 位、金额 2 位，不是一条**：库里 `products.default_unit_price` / `cost_price` / `special_unit_price` / `order_products.unit_price` / `ledgers.unit_price` 是 **`Numeric(14,4)`**，而 `cash_flows.amount` / `expenses.amount` / `shipper_receipts.amount` / `orders.freight_fee` / `users.salary` 是 **`Numeric(12,2)`**。用 2 位去卡单价 = 把"定价 12.3456 元"悄悄封掉（这条差点改错）。<br>⚠️ **全角数字要归一成半角，不能丢**：`Char.isDigit()` 是 **Unicode** 判断（`１３８` 会被放过，而 Python 的 `\d` 同样是 Unicode —— 两边一起放行一个打不通的号）；反过来"只认 ASCII、其余丢掉"也错（`moneyInput("１.５")` 只剩一个点儿、算出 `0.`）。现在是 `asciiDigit()` 映射 U+FF10~FF19。<br>⚠️ **粘贴 `+86 138…` 要剥国家码**：不然会变成 `86138000000` —— 11 位、看着正常、打不通，而联系电话规则（7~12 位）会放它过去。只在"正好 13 位且以 86 开头"时剥（`186…` 的 11 位手机号不受影响）。<br>⚠️ **登录框是唯一必须放开的例外**：它收的是「手机号**或**用户名」，用户名可以是字母 —— 加数字过滤会把用字母用户名的人挡在登录页外。<br>⚠️ 电话规则**故意收座机**（7~12 位，**不要求**"11 位必须以 1 开头"）：`01012345678` 是合法的 11 位座机，加了那条会把所有座机挡掉。<br>⚠️ 改完跑 `_tools/qa/_reverse_verify_input_rules.py`（含"注释里写着 InputRules 但实际不过滤"那条假绿，以及"下单页又把单价框加回来"） |
| **按「人」搜索**（姓名 / 手机号 / **手机号后 4 位**） | **后端唯一实现**：`app/core/user_search.py::name_or_phone_like(name_col, phone_col, kw)`（两个消费点：`api/v1/users.py::list_users`、`api/v1/customers.py::list_customers`）。<br>**App 侧唯一实现**：`core/UserSearch.kt`（`HINT` 提示文案 / `matches` / `filter`）+ 共享控件 `ui/common/Components.kt::SearchField` | 账本仪表盘（`DispatcherLedgerViewModel` 本地过滤 `/ledger/accounts`、`/freight-settlement` —— 这两张**一次回全量**，不走服务端反而更快）；四个名册页（司机/货主/批发商/账户管理）走**服务端** `?q=`（`AppRepository.usersPage(q=…)`，一页上限 500）；`VehicleManageScreen` 的司机候选、`AddressScreen` 的「联系人」段也用同一份 | 用户 2026-09-19 原话：「这个搜索键可以根据他们的**名称**还有**电话号码**以及**电话号码的后 4 位**进行搜索」。<br>⛔ **后 4 位不是额外一条规则**：手机号走子串匹配，`8001` 天然命中 `13800008001`。**不许**写"取后四位再比"的分支 —— 多一条分支就多一个会跟主规则分叉的地方，而分叉的表现是"搜得到/搜不到"（用户只会觉得系统坏了）。<br>⛔ **名册页必须打服务端**：`/users` 一页最多 500 条，本地过滤时第 501 个人在客户端**根本不存在** —— 而"列表里没有"会被读成"没有这个账号"→ 再建一个 → 撞手机号唯一约束。账本那两张接口一次回全量，本地过滤才成立（"两张表看起来是两套实现，其实是同一条规则按数据是否完整分流"）。<br>⚠️ 搜索命中**不许覆盖名册**（`hits` 与 `users` 分开）：`driverNameOf` / 车队摘要 / 配车弹层要完整名册，覆盖了会出现「账号 #5（不在司机名册里）」这种假故障。<br>⚠️ 大小写两侧都要对齐（后端 `func.lower()`、App `ignoreCase = true`）：MySQL 的 `like` 默认不分大小写、SQLite **区分**，不写就是"开发库能搜到、生产库搜不到"。<br>⚠️ **地址搜索是另一条规则**（提示语里带地址/线路/地点的框），套上只认姓名/手机号的规则会让「按地址找线路」直接失效。<br>红线 `_tools/qa/_check_user_search.py`（30 项，**自己从源码算**出所有"提示语同时提人和号码"的框）+ 反向验证 `_tools/qa/_reverse_verify_user_search.py`（25 种注入）+ 单测 `core/UserSearchTest.kt` |
| **列表端点的分页与截断**（**唯一出口**） | 后端 `app/core/pagination.py::finish_page(rows, limit, response)`；**App 侧** `data/repo/AppRepository.kt` 的 `PageMeta` / `parsePageMeta()` / `Response<*>.pageMeta()` / `Response<List<T>>.pageRows()`，文案 `ui/common/Components.kt::truncationHint()/TruncationNote()` | 8 个有 `limit` 的端点全调 `finish_page`：`orders`（`_orders_response`）、`notifications`、`ledger`、`cash_flows`、`inventory`、`operation_logs`、`places`、`users`；App 侧 9 个页面读头：库存流水 / 现金流水 / 审计日志 / 共享地点 / 账号名册（两个页面）/ 派单员账本 / 货主账本 | ⛔ **两边都不许各写一份**（原来就是这么漏掉 5 个端点的）。两条红线**自己算清单**盯着：`_tools/qa/_check_pagination_wiring.py`（后端：有 `limit` 就必须回报截断 + 取数 `.limit(n + 1)`）、`_tools/qa/_check_page_truncation_wiring.py`（App：每个列表页必须调 `pageMeta()` 并把提示渲染出来）。<br>⚠️ 不缺这半边会怎样：现金流水页把"一页 200 条"当总额（实测少算 **62%**）、审计页以为"这条改动没被记录"、账号列表找不到人再去建一个（撞唯一约束）、账本页的合计只含取到的那一页。<br>⚠️ `X-Truncated`/`X-Result-Limit` 必须留在 CORS `expose_headers` 里（跨源时浏览器只让脚本读被显式暴露的头）。<br>⚠️ **上限读不到时不许编数字**（老后端没有这两个头 → `PageMeta.ABSENT` → 只说"服务器没回报条数"） |
| **时间基准**（**全库一律 UTC**） | `app/core/business_time.py`（`utc_now_naive` / `business_date` / `business_day_start_utc`）、`app/models/base.py::TimestampMixin`（**Python 写** `default=utc_now_naive`）、`app/database.py`（MySQL `connect` 钩子 `SET time_zone = '+00:00'`）、**Android** `util/TimeFmt.kt` | `app/models/operation_log.py::created_at`（不走 mixin，同样口径） | ⛔ **不许再用库端时钟**：`server_default=func.now()` / `onupdate=func.now()` 在生产是 **+08:00**（会话 `SYSTEM`），与 Python 写的 UTC 差 8 小时 → 「待派超时 4 小时」实际 12 小时、保留策略与审计窗口整体偏 8 小时。**本机 SQLite 两边都是 UTC，所以单测全绿**（这条缺陷就是这么活的）。红线 `_tools/qa/_check_time_base.py`：凡库端时钟列必须同时有 Python `default=`，且 `database.py` 必须有那个会话时区钩子。<br>⚠️ 口径是"**UTC 存、按业务当地日分桶**"（`BUSINESS_TZ` 东八区）——不要把存储基准和报表分桶混成一件事。<br>⚠️ **存量数据**：代码收口后老行仍是 +08:00，一次性平移脚本 `_tools/deploy/_shift_times_to_utc.py`（默认 dry-run、要备份确认、幂等标记、时差问库不写死）<br>⚠️ **客户端侧也有这一条**（2026-09-19 修，`_tools/qa/_check_time_base.py` 现在也扫它）：后端把 naive datetime 序列化成 `"2026-09-19T10:09:36.713251"`（**没有时区后缀**），而 `util/TimeFmt.kt` 原来用 `OffsetDateTime.parse` 解析它 —— **必然抛异常**，于是每一处都掉进兜底分支、把 **UTC 原样印出来**：真机（东八区）上所有时间**早 8 小时**，格式与排序都正常所以看不出来。现在 naive 串按 UTC 解释再换算到设备时区（`parseBackendInstant`），单测 `TimeFmtTest` **显式传时区**断言（靠 `ZoneId.systemDefault()` 会跟着跑测试那台机器变）。⚠️ 本机模拟器时区恰好是 UTC，所以这个缺陷**只在真机上暴露** |
| **图片文件的清理**（**唯一判据**） | `app/services/image_archive.py::purge_orphan_images` + `REFERENCED_IMAGE_COLUMNS` | 红线 `_tools/qa/_check_image_refs.py` | ⛔ 删文件的判据是"**还有没有任何一行指着它**"（**软删行也算**）：漏一列 = 删掉还在用的图且不可恢复。`delivery/` 刻意不在扫描范围（送达凭证的寿命绑在订单上，由 `delete_orders_by_ids` 按单清）。归档前先按 header 里的尺寸判 `MAX_ARCHIVE_PIXELS`（解码峰值 = 宽×高×通道，而尺寸是客户端说了算） |

### 1.6 基础设施（改这些要格外小心）

| 文件 | 行数 | 说明 |
|---|---|---|
| `app/core/schema_bootstrap.py` | **1057** | 建表 + MySQL 迁移补丁。**历史事故高发区**：orders.status 枚举、ledgers.source 枚举、fcntl 文件锁都在这里。新增迁移代码**必须写在 `with engine.begin()` 作用域内**，否则启动崩溃循环 |
| `app/services/data_retention.py` | 340 | 数据保留策略总入口（3 年保留 / 软删 30 天物理清理 + 无引用图片清理），挂在 `lifespan` 上。<br>⚠️ 入口自带**跨进程互斥**（`_single_runner`，`LOCK_NB`）：多 worker 下拿不到锁的进程**跳过本轮**，而不是排队等它跑完再原样跑一遍（2026-09-19 外部完整检查 PERF-05） |
| `app/core/rbac.py` | 117 | **角色权限矩阵**（`Permission` 枚举 + `ROLE_PERMISSIONS`）。新增权限点从这里下手。<br>⚠️ 但它是"角色→权限点"的表，**不是"端点→谁能调"的表**：`role_has_permission()` 开头就对**派单员一律放行**（L109-L117），且多数端点根本没挂 `require_permission`。端点到权限的真相看 [`08A_ENDPOINT_INDEX.md`](08A_ENDPOINT_INDEX.md) |
| `app/deps.py` / `app/core/security.py` | — | 依赖注入与 token |
| `app/database.py` / `app/config.py` / `app/redis_client.py` | — | 连接与配置（`.env` 可覆盖） |

---

## 2. Android（`android/`）

> 以下路径省略公共前缀 `android/app/src/main/java/com/tapmoay/sorders/`
> （该前缀已在**文件第 3 行**用 `ref-prefix` 声明给 `check_refs.py`；放在顶部是因为第 1 节的后端行里也会引用 Android 文件）。

### 2.1 导航与角色（改结构先看这里）

| 功能 | 核心文件 | 说明 |
|---|---|---|
| **角色 → 底部 Tab / 模块入口** | `ui/nav/Modules.kt` | **单一真相源**：`entriesFor(role)` + `BottomTab`。文件内注释写明"未来加 Tab 只改这里"。<br>**2026-09-19 派单端工作台调整**（用户：「将 pai 工作台里的派单那个图标给去掉，也去掉这个将里面的计费模板和计费规则，给移出来做一个 2 个单独的图标放在工作台里面」）：<br>· ⛔ **删掉「派单作业」分组**（它原来点进去是 待派单池/全部订单/订单模板/计费规则）—— 去掉是安全的：待派单池**就是底部导航第一个 Tab**（同一页两个入口），"全部订单"与「订单管理」图标**指向同一条路由**。删掉之后派单端工作台正好 **4×4 = 16 格**。<br>· 「运费模板」「计费规则」提成**两个独立图标**。⛔ 名字**没有**照抄用户口述的"计费模板"：它和旁边的"计费规则"只差一个字，并排摆两个几乎同名的图标，用户每次都得想一下点哪个；这一页自己的弹窗与字段本来就叫「新建运费模板」「一车价格」，所以取**运费模板**（两者含义本来就不同：一个是**订单运费**的价目表，一个是**司机拿多少**的规则）。页标题也跟着从「订单模板」改成「运费模板」（图标与页面对不上更糟）。<br>· 两个新色都有依据（与工作台所有已有色的 RGB 欧氏距离最小值，判据 ≥60）：运费模板 = 深靛 `#283593`（**121**，与报表的亮靛 `#6950F5` 同族不同深浅）、计费规则 = 橄榄 `#9E9D24`（**80**，与「司机管理」的黄绿同族 —— 都指"司机这块"）。⚠️ 计费规则**不能**继续用 `#CDDC39`：它当子项时无所谓，提到工作台顶层就与「司机管理」**完全同色**（`ModulesEntryTest` 会红）。<br>⚠️ 分组的**机制**还在（`ModuleEntry.children` + `Routes.MODULE_GROUP` + `WorkbenchScreen.kt::ModuleListScreen`），但本轮之后**没有任何网格用它**（三端 children 全空）——别以为它是活的 |
| **路由定义** | `ui/nav/Routes.kt` | 所有路由常量 |
| **导航图** | `ui/nav/NavGraph.kt` | 页面注册与跳转 |
| **语义色体系** | `ui/theme/` | 一色一功能（见 `06_DESIGN_SYSTEM.md`） |
| **外观模式（白天/夜间 + 随日落自动，**按手机定位算**）** | `ui/theme/Theme.kt`（`ThemeMode`：明暗 + 自动开关，都落盘）、`ui/theme/AutoSunTheme.kt`（定时器，挂**根节点**）、`core/SunClock.kt`（**几点算天黑**，纯函数 + 单测）、`core/SunLocation.kt`（手机在哪：校验 + 兜底）、`core/AmapLocationManager.kt`（定位来源①：高德，**带地址**，成功即喂给 SunLocation）、`core/DeviceLocation.kt`（定位来源②：**系统定位兜底**，只喂 SunLocation）、`ui/home/RoleHomeScreen.kt`（定位到手就重算一次；高德失败/起不来时退兜底）、`ui/profile/ProfileScreen.kt`（三端共用的开关行） | ⚠️ 四处最容易改坏：① 定时器必须挂 `MainActivity.setContent`（挂页面里就只有停在那页才切）；② `MainActivity.onResume` 必须补一次对表（后台过夜进程被冻结，`delay` 不会醒）；③ 自动模式下必须**禁掉**手动开关（否则"点一下又自己弹回来"）；④ **定位只是精度增强**：拿不到坐标必须退回时区估算（`SunClock.stateHere`），且界面要如实说"按定位/按时区估算"——高德定位失败会回调 `(0,0)`，不校验就会算出错 8 小时的日落且不报错<br>⚠️ **两条定位来源分工不许混**：`DeviceLocation` 是**系统**坐标（WGS-84），只准喂 `SunLocation`（日落对几百米不敏感）；地址/拍照水印要的是**高德的 GCJ-02**（与后端/Web 一致），所以它**不许**往 `AmapLocationManager.locations` 里发点。红线 §21 有两条钉着（一条 glob 全树算 `SunLocation.coords()` 的消费点，必须只有 `SunClock.kt`） |
| **运费分类名册 + 运价匹配 + 待定价**（2026-09-21 这一轮） | **后端**：`app/services/freight_pricing.py`（**匹配的唯一实现**：路线 + 分类 + 司机 → 唯一一条价目；多条同样优先 → `ambiguous` **不猜**）、`app/api/v1/freight_categories.py`（名册 CRUD，照商品分类那一套）、`app/api/v1/freight_templates.py::quote_freight`（`GET /freight-templates/quote`）、`app/api/v1/orders.py::price_freight`（`POST /orders/{id}/price-freight`：手动定价 + 沉淀）、订单列表的 `?unpriced=true`（待定价）<br>**Android**：`ui/dispatcher/FreightTemplatesScreen.kt|ViewModel.kt`（左分类栏 + 卡片：**路线与价格最大**）、`FreightCategoriesScreen.kt`（分类管理）、`FreightPricingScreens.kt`（待定价列表 + 定价弹层）<br>**红线**：`_tools/qa/_check_freight_pricing.py`（66 项）+ 反向验证 23 种注入 | `app/models/freight_category.py`、`app/models/freight_template.py::FreightTemplateCategory`（一条价目挂多个分类）、`docs/PROJECT_MAP/06_DESIGN_SYSTEM.md §4.18` | 用户 2026-09-21：「运费模板**有自己的一套分类**（复用商品分类的代码和方法）」「按地点库的路线定价」「没匹配到就没有定价 → 派单员**手动定价**，定价完之后**新增对应的路线和对应的运费模板**并绑到那个分类」<br>⛔ **价目归计费规则**（2026-09-21 用户第二轮口述：「运费模板**不会去匹配车型也不会匹配司机**，匹配车型和匹配司机在**计费规则**中……这一目录就归这个计费规则，而这个规则在匹配对应的司机」）：链子是 **价目（路线+分类+价格）→（规则里勾选）→ 规则（车型 + 多少钱）→（挂在司机身上）→ 司机**；派单选了司机 → 他的规则勾的价目里按「路线 + 分类」挑一条 → 运费。规则里勾价目的界面**照选品页**（左分类栏 + 可全选本分类 + 可单独勾）。<br>⚠️ 规则卡上的价目**不许全名全列**（勾 8 条就占 4 行、把卡片撑高一倍，还一个价格都看不到）：出参给 `template_briefs`（`_template_briefs`，形如「惠州江北 → 东莞樟木头 ¥62.00」，**零额外查库**），卡片写成「价目 N 条：前两条 等」；**没勾价目要显式红字**说"派给这个司机的单会进待定价"（沉默 = 用户以为配好了，见 §4.18 第 6 条）<br>⚠️⚠️ **`GET /orders?unpriced=true` 与 `list_orders` 的两条查询路径**（2026-09-21 真机抓到的静默 bug）：这个端点有**两套互不相干**的查询构造（派单员+搜索词走 `stmt`、普通列表走 `q`），**每一个过滤条件都要各写一遍** —— `unpriced` 曾经只加在前者上，后果是**待定价页（不带 `q`）静默返回全部订单**（页面上写着"这些单已经派出去了、但还没有运费"，用户照着核对时看到的是有运费、甚至已撤销的单）。现在红线 ⑤ **数两条路径**（4 个片段各须出现 ≥2 次）+ 行为测试 `tests/test_freight_pricing.py::test_待定价过滤在两条查询路径上都生效` 真打端点<br>⚠️ 定价弹层是**两级兜底**（2026-09-21 第四轮）：先 `driver_id = 这一单的司机`（沿用他的规则勾的价目），没有再 `driver_id = null`（退回运费模板，只看路线 + 分类）—— **复用同一个 `/freight-templates/quote`**，别为此新开端点；同样优先多于一条时**不猜**，列出来让人点一条<br>⚠️ **现状（2026-09-21，等用户拍板）**：`quote_for` **只被 `/freight-templates/quote` 和定价弹层调用 —— 派单/下单都还没接它**，所以运费目前一直是"下单时人工填"，"选司机自动带价"这条链子**还没接到业务流上**（全库也 0 条 `freight_fee is null` 的存量单，待定价页平时是空的）。要接的话只准调这一个函数，且"货主填的价 vs 价目标准价谁优先"是**动客户付的钱**，先拍板<br>⚠️ **匹配只许一处**：`quote_for` 是**唯一实现**（当前消费点：`/freight-templates/quote` 报价端点 + 待定价的定价弹层）；散开的表现是"同一单两个价，两边都不报错"。⛔ **派单 / 下单流程目前还没接它**（见上面那条"现状"）<br>⚠️ 价目表上**没有**车型与司机两项了（旧列还在库里但不再参与匹配）；沉淀出来的价目会**自动勾进这一单司机的规则**（否则「下次自动带价」是假的）<br>⚠️ **同样优先多于一条 → 不猜**（报 `ambiguous` 让派单员挑）：价目配歧义要在配置时就看得见<br>⚠️ "不知道司机/分类"的那一维**不参与筛选**（给中性分）：否则待定价页会把绑了司机的价目全判成不可用，明明有价也报"没有价目"（测试抓到）<br>⚠️ 分类**按编号**挂（改名天然安全）；订单上存"编号 + 名字快照"（历史单据要能独立复核）<br>⚠️ 没匹配到**不是异常单**：`is_exception` 是人工标的业务异常，混在一列只会让两边都看不清 |
| **手动定价能"沉淀"**（同上一行，单独记一笔因为它改钱） | `app/api/v1/orders.py::price_freight`（写运费 + 分类快照 + 建/找线路 + 建/改价目 + `ORDER_FREIGHT_PRICE` 审计）、`ui/dispatcher/FreightPricingScreens.kt`（勾「存成价目」） | `models/freight_template.py`、`models/shipper.py::ShipperAddress`（线路） | 沉淀 = ① 按订单送货地址建/找一条**线路**；② 在它上面建/改一条**绑了这个分类**的价目。<br>⛔ 沉淀**只影响以后的单**：已经派出去的单记的是当时的运费，不是引用（改价目不许回写历史单）<br>⚠️ 定价**不许**顺手改异常标记（那是另一件事） |
| **司机计费规则（固定工资/每单/提成，可挂给司机）** | **后端**：`app/services/driver_pay.py`（**钱的唯一算法处**：`order_pay` / `pay_for_order` / `snapshot_mode` / `dispatch_mode` / `override_problem` / `monthly_salary_of`）、`app/models/driver_billing_rule.py`、`app/api/v1/driver_billing_rules.py`（CRUD + `attach`）、`app/schemas/driver_billing_rule.py`（**校验只有一份**）<br>**别人调它**：`services/accounting_service.py::generate_piece_bill`（送达生成账单）、`api/v1/driver_bills.py`（补单 + 月薪单）、`api/v1/freight_settlement.py`、`services/stats_service.py`、`api/v1/reports.py`（导出）<br>**Android**：`ui/dispatcher/DriverBillingRulesScreen.kt`（模板管理）+ `UsersManageScreen/ViewModel`（给司机挂规则）<br>**AI**：`ai/AiWriteBasicData.kt`（五个动作）、`ai/AiResources.kt` + `ai/AiRevert.kt`（撤回 / 撤不回来的理由） | ⚠️ **"这个司机这单拿多少"只有 `driver_pay` 一处实现**，上面五个消费点谁都不许再自己抄 `freight_fee`（红线 §22 逐点断言）；<br>⚠️ 派单时把规则**快照**到 `orders.driver_rule_snapshot`——改规则**不追溯**已派单；<br>⚠️ 挂载只有一条写路径（`POST /driver-billing-rules/attach`），`PATCH /users` 刻意改不了它；<br>⚠️ **逐单覆盖**（这一单单独的金额 `driver_piece_amount` / 比例 `driver_commission_rate`）与两份快照由 `order_flow.assign_driver` 一处写、且**先验再做**：司机没挂规则、或规则里没有提成项时直接 400（判据 `driver_pay.override_problem`），不许"填了不生效"（界面有数字、账单另一个数）；逐单定额还会把这张单的模式快照写成 PIECE——否则那笔钱不生成账单、静默消失；<br>⚠️ 抽成范围（`commission_product_ids`）只在"按商品金额抽成"上合法，手工加的商品行（无 `product_id`）在限定范围时不算；<br>⚠️ **派单端"要不要给这个司机显示运费框"的判据不许自己猜**（报告 P0-3）：后端在 `UserOut` 上给了 `pays_per_order`（= `driver_pay.snapshot_mode(u) == "PIECE"`，规则优先，与账单/`freight_visible` 同源），安卓侧只读它。原来客户端按 `billing_mode ?: 车型` 判断（= `resolve_billing_mode`，**少了"规则优先"这一层**）→ 挂着**运费提成**规则的大车司机被判成工资制 → 运费框不显示、运费为空、提成算成 0 → 送达时 `pay.total <= 0` **连账单都不生成**。另：派单请求里的运费**填了就发**，不许按这个判据去丢用户的输入 |

### 2.2 网络与实时（改动影响全端）

| 功能 | 核心文件 | 注意 |
|---|---|---|
| **后端地址解析** | `core/ApiEndpoint.kt` | 模拟器必须走 **10.0.2.2**；此文件已做自动探测切换 |
| **HTTP 客户端** | `core/ApiClient.kt` | 拦截器、鉴权头 |
| **全部接口定义** | `data/remote/api/Apis.kt` | 加/改接口先动这里 |
| **全部 DTO** | `data/remote/dto/Dtos.kt` | 与后端 schema 对齐 |
| **仓库层** | `data/repo/AppRepository.kt` | 页面与网络之间的唯一通道。<br>⚠️ **列表页读响应头只有一处**：`PageMeta` / `parsePageMeta()` / `Response<*>.pageMeta()` / `Response<List<T>>.pageRows()`（本文件 632-692 行）。新加列表页**必须**走它（拿 `List` 不复核元数据 = 把"一页"当成"全部"），否则 `_tools/qa/_check_page_truncation_wiring.py` 会红 |
| **Socket 实时** | `core/SocketManager.kt`（连接/握手/`connect_error` 分类/`session_revoked`）+ `core/RealtimeHub.kt`（事件分发）+ `core/PushTrust.kt`（**推送可信判据**：收件人是不是我 / `type` 白名单 / 单号边界 / 握手算不算"被服务端拒绝"，纯函数有单测） | **踩坑**：① `connect` 必须在会话恢复时也调用，只在登录时 connect 会导致重启后收不到推送；② 负载是 `JSONObject`，**嵌套对象/数组必须递归转 Map**（`plain()`），只转外层会让站内信的 type/title/单号被静默丢掉；③ 判据**只有 `PushTrust` 一处**：派单员的 `role_dispatchers` 广播**没有 `recipient_id`**（按它拦会把派单员的新单提醒全杀掉），握手被拒要清会话而不是无限重连、网络抖动**不许**当成被拒 |
| **通知与语音提醒**（系统通知/来单了/待派单/后台常驻） | `core/NotifyCenter.kt`（渠道与发通知）+ `core/NewOrderAlert.kt`（**纯判定，有单测**：**角色 × 事件类型两维**——司机听 `order.assigned`、派单员听 `order.created`）+ `core/NewOrderPlayer.kt`（播报）+ `core/AlertService.kt` + `core/BootReceiver.kt` + `core/AlertPrefs.kt` | 设置页 `ui/profile/AlertSettingsScreen.kt`（入口在「我的」）；素材 `res/raw/new_order.wav`（司机）+ `res/raw/pending_order.wav`（派单员），生成脚本 `_tools/media/_gen_new_order_clip.py --kind {driver,dispatcher}`（**默认音色＝用户选的晓晓/女声**，换错音色不报错，所以另有 `_tools/media/_probe_clip_voice.py` 量基频、红线对账）；⚠️ **派单员那条的打断在 `ui/dispatcher/DispatcherPoolViewModel.kt`（派单成功立刻 stop）**；红线 `_tools/ai/_check_notify_guardrails.py`；**改这层先看 [INTEGRATIONS.md §5](../INTEGRATIONS.md)** |
| **底部导航栏（含中间凹口）** | `ui/home/RoleHomeScreen.kt`（接线）+ `ui/nav/NotchedNavBar.kt`（**凹口几何是纯函数，有单测**）+ `ui/nav/Modules.kt` 的 `AiNavButton`（圆钮尺寸/凸出常量） | 有 AI 圆钮的角色才切凹口形状；**凹口画错了不会报错、界面上也看不出来**，所以配了像素量尺 `_tools/notify/_measure_notch.py`（量缺口深浅 + 边缘爬起来的速度）、已知答案的对照图 `_tools/notify/_notch_render.py --check` 与红线 `_tools/ai/_check_nav_guardrails.py`。凹口圆心在栏**内侧**（半径−凸出），放在栏外会切出一个藏在圆钮后面的浅坑。**两侧的角必须是与上沿、大圆双相切的圆角，而且半径只有唯一正确取值 = 圆心深度**（切点才落在最宽处，旧的方角才会整块消失）——用户 2026-09-20 指的就是这个（「2 个矩形像是框在一起…把它做一个曲线过渡」），见设计系统 §4.11 |
| **工作台（图标网格，**没有卡片**，可长按拖动排序）** | `ui/home/WorkbenchScreen.kt`（**图标格的唯一实现**：`WorkbenchTile`；**没有卡片外壳**）＋ `core/WorkbenchOrder.kt`（**顺序的唯一一份规则**：`move` / `apply` / `keys` + `WorkbenchOrderStore`）＋ `ui/nav/Modules.kt`（**入口清单只有这一份**：`dispatcherEntries` / `shipperEntries` / `driverEntries` / `ledgerHomeEntries`）＋ `ui/common/EntryGrid.kt`（**入口页版式**：`EntryCardGrid`，报表中心与账本管理共用） | **2026-09-20 用户第三轮**：①「以前是**没有卡片的**」→ 那一层 `Surface` 整个拿掉，图标直接铺在页面背景上；②「以前的图片图标啊，它是**可以随意拖动**的，就像那个桌面图标一样」→ **长按 300ms 后拖动、就地交换、松手落盘**（`WorkbenchOrderStore`，`SharedPreferences`，**按角色分开存**）。<br>⚠️ 拖动的两个坑：**落点必须用绝对坐标**（`dragPos − 这一格现在的中心`；用位移增量的话换过一次位置图标就跳开手指）、**`pointerInput` 的 key 只能是 `entry.route`**（把 `entries` 也放进去 = 换一次位置手势被取消重建 = 拖一格就断）。<br>⚠️ `onDragEnd` **与** `onDragCancel` 都要落盘（顺序在拖动过程中已经改了，只是手势被系统抢走时不存 = 用户下次进来顺序变回去）。<br>⚠️ `graphicsLayer` 里**先判是不是正在拖**再读 `bounds`（否则 17 格每帧都参与状态依赖，整屏白白重算）。<br>⛔ 工作台**没有分组机制**了：`ModuleEntry.children` / `findGroup` / `Routes.MODULE_GROUP` / `ModuleListScreen` 已删（三端 children 全空 = 一条走不到的死路由）。<br>⛔ 网格里要有「账本管理」（→ `Routes.LEDGER_HOME`）与「车辆管理」（→ `Routes.DISPATCH_VEHICLES`）两格；⛔ 开销管理/司机结算/4 类账**只许在账本管理入口页里出现一次**；⛔ 末行要补空位；⛔ 格子的色要跟邻居同亮度同饱和、只差色相。红线 `_tools/qa/_check_ledger_dashboard.py`；顺序规则单测 `core/WorkbenchOrderTest.kt`；颜色判据在 `ModulesEntryTest` |
| **登录态存储** | `core/TokenStore.kt` | DataStore；`sessionFlow` 是**冷流**，注意 `initial=null` 的瞬时态 |
| **依赖容器** | `core/AppContainer.kt` | 手动 DI |

### 2.3 派单员工作台（`ui/dispatcher/`，36 文件，最大业务面）

| 功能模块 | Screen / ViewModel |
|---|---|
| 派单作业（待派池） | `DispatcherPoolScreen.kt` / `DispatcherPoolViewModel.kt` |
| 订单管理 | `DispatcherOrdersScreen.kt` / `DispatcherOrdersViewModel.kt` |
| 账本管理 | **入口页** `ui/dispatcher/LedgerHomeScreen.kt`（报表中心那种形式，版式见 `ui/common/EntryGrid.kt::EntryCardGrid`）→ **账本页** `DispatcherLedgerScreen.kt` / `DispatcherLedgerViewModel.kt` + `ui/dispatcher/LedgerCharts.kt`（**图表取数纯函数，有单测**）。<br>**2026-09-20 定稿：工作台一格 → 入口页 → 账本页只看账**。用户为这一块改了**三轮**，最后一句把前两轮都推翻了：「派单员的那个工作台全部改一下，**改回原来的样式**……首先，我们将**司机的账和司机结算**这 2 个东西**合并成一个**；然后订单账本，再加上货主账本以及批发商账，还有客户收款以及开销管理，**合并成一个形式，就叫做账本管理**，这个账本管理**类似于报表中心的形式**；然后车辆台账属于车辆管理，车辆管理直接放在桌面上就行了」（被推翻的两轮：账本页左栏版、工作台第二张卡片版）：<br>　· **工作台回到单网格**（`WorkbenchScreen` 只有一张卡片）；网格上有「账本管理」一格（→ `Routes.LEDGER_HOME`）与「车辆管理」一格（→ `Routes.DISPATCH_VEHICLES`，就是原来的车辆台账）。<br>　· **入口页 6 格**（`Modules.ledgerHomeEntries`，**唯一一份清单**）：订单账 / 司机账 / 货主账 / 批发商账 / 客户收款 / 开销管理；4 类账走**同一条带参数的路由** `Routes.dispatcherLedger(tab)` → 账本页 `?tab=` 直达。<br>　· ⛔ **「司机结算」不单独占一格**（用户点名「合并成一个」）：入口页 6 格里没有它，**账本页里也不再挂入口**（第四轮：「那个结算，这个也直接去掉」）—— 它走工作台那一格 `Routes.FREIGHT_SETTLEMENT`（红线里有一条断言专门防「顺手把功能删了」）。<br>　· ⛔ **入口页与报表中心共用同一份版式**（`EntryCardGrid`，全项目只许一处定义）：抄一份的话两页迟早长得不一样（见设计系统 §4.13）。<br>　· ⛔ **入口页 6 格与工作台网格不许重复**：开销管理/司机结算/4 类账都**只在入口页里出现一次**。<br>**第五轮定稿（当前这一版）：一类账一页，整页从上到下 = 时间（顶栏药丸）→ 人员那一行（点开侧边抽屉）→ 数据；这一页一张图都不画**。用户原话：「最上面的 4 个去掉，那是**老的导航栏**…整个排版是**先是搜索**，然后再是**时间**，最后在下面再就是**选择司机**；先选择司机，下面的数据才会出来，**不过默认是全部**。然后他会按照司机对**下面的图**进行匹配和订单匹配，**包括其他的货主以及批发商都是一样的**」。<br>　· ⛔ **页内那条 4 页签导航（`LedgerTabBar`）与 `selectTab` 已删**：4 类账各有各的入口格，页内再切一遍＝同一个东西两个入口；档位改由构造参数 `initialTab` 定死（`private set`），顶栏标题写的就是这一类账（`vm.kindTitle()`）。<br>　· **选人 = 侧边抽屉**（`ModalNavigationDrawer` + `PersonDrawer`）：页面上只留一行「人员：全部（N 人）」（`PersonTriggerRow`）当入口，抽屉里有搜索框 + 名单（第一行「全部」，重名时缀手机号后 4 位），选中即关；点账户行＝进他的账（同一个状态，`openPerson`/`selectPersonKey` 一条路）。<br>　· **时间 = 顶栏紧凑药丸**（`DatePresetPill` + `DatePresetDialog`，都在 `ui/common/Components.kt`）：写当前窗口、点开是一列档位。⛔ 页面**不用** `DatePresetRow` 那一行胶囊（用户：「太复杂了……换一种崭新形式」，而且「时间和选择人物不要选择一样的展现形式」）。<br>　· ⛔ **账本页一张图都不画**（第五轮：「折线图条形图还有扇形图，直接去掉就行了……在报表中心看就可以了」）：`LedgerCharts.kt`（取数纯函数）/ `ChartPalette` / `PieChart` **整块删除**；`ui/common/Charts.kt` 的折线/条形**留着**（报表中心、货主账本、司机端在用）。<br>　· ⚠️ 抽屉里的搜索**只筛名单、不改页面合计**（`dashboard()` 接的是 `accountRows()`）：边打字边改合计，关掉抽屉就剩一个对不上的总数。<br>　· **默认时间档位落在「有数」的那一天**（用户 2026-09-20 尾：「这些时间默认是今天的，如果今天没有任何订单的话，然后再是昨天，以此类推」）：初值=今天，打开后异步退档 `今天 → 昨天 → 前天 → 近 7 天 → 全部`（`AUTO_LADDER` + `periodHasData`，判据与页面取数同源）；点开某个人时也一样（`fallbackForPerson`）。⛔ **用户自己挑过档位（含手输区间）之后永不再自动改**（`userPickedPreset`，两处退档共用这一个开关）。<br>　· **第二层（某个人）**：`LedgerPersonScreen.kt`（三个数 + 商品统计 + 逐单核销）＋ `LedgerPersonStats.kt`（商品统计纯函数：`productStats`/`lineReceivableCents`/`orderReceivableCents`，有单测）。司机那一层不一样：他的钱是 `driver_pay` 口径（我们该给他多少）、**没有核销**，而且那几单已经在列表响应里（不再请求）。<br>　· **批量核销（点合计）**：选中某个人之后，他那一张卡的「合计」下面有「核销全部（N 单 · ¥X）」—— 一次收款单带全部 `order_ids`；⛔ **「全部人」那一层不给批量核销**（收款单绑的是某个客户的档案，在那一层收钱会记到错的人头上）。金额 = 各单 `arrears_amount` 之和（后端算的），界面只显示不重算。<br>　· **整单核销的金额 = 这一单还欠的钱**（`accounting_service` 里 `else m.arrears`）：按商品核销过一部分的单（`paid=False`、欠 700/应收 1000）原来后端按 1000 算，客户端按 700 发 → 两边金额对不上（这种单再也收不动）；凑巧对上就是**多收**。<br>　· 原来顶部那"一排 4 个工具按钮"（`AccountToolsEntry`/`ToolChip`）连同左栏版（`LedgerRailEntries`/`MasterRail`）**都已删除**。<br>**日期档位**（第一格就是**「全部」＝不带日期条件**）：全部/今天/昨天/前天/近 7 天/上周/本月/上月 + 自定义。档位与区间**只有一份实现** `ui/common/DatePresets.kt`（`rangeOf(label, today)`，纯函数有单测），胶囊那一行只有 `ui/common/Components.kt::DatePresetRow` —— 通用筛选条 `DateRangeFilter` 与账本页共用它。<br>　· ⚠️ 口径写死在这两处：**本月 = 本月 1 日 ~ 今天**（未来的日子没有账）、**上周 = 上周一 ~ 上周日**（不是"最近 7 天"）。各写一份的后果是"账本页的本月与筛选条的本月差几天"，两边都看着对。<br>　· ⚠️ 「全部」在**结算接口**上要给一对宽到没有实际边界的端点（`2000-01-01`~`2099-12-31`）：那个接口 `from/to` 必填，不给直接 400；账户汇总接口相反 —— 省掉 `from/to` 才是**真·全部**（别传一个假区间进去）。<br>**三种图**（`ui/common/Charts.kt`：`LineChart` / `BarChart` / `PieChart`，切换条走共用的 `SegmentedStatusTabs`）：折线/条形 = 按天，扇形 = 构成。取数一律走 `LedgerCharts.kt` 的纯函数（`dailySeries` / `topSlices` / `orderDailySeries` / `orderSourceTotals` / `driverDailySeries` / `driverTotals` / `accountTotals` / `chartTypesFor`）。<br>　· ⛔ **货主账 / 批发商账不给折线**（`chartTypesFor(2\|3)`，有单测）：`/ledger/accounts` 只回账户汇总、没有按天的数；拿明细接口去凑一条曲线，会在明细被截断时画出**比上面合计小**的线 —— 同一屏两个数，比"少一种图"糟得多。这两档给「排行 + 构成」。<br>　· ⛔ **图上的数必须与合计同源**：图自己再聚合一遍就会出现「上面写 ¥3,200、扇形加起来 ¥2,800」。所以页面里**不许出现 `moneyToDouble(`**（那条判据真的拦过一次注入）。<br>　· ⛔ **扇形环心那个数 = 切片之和**（不是从别处塞一个合计进来）；切片**跟着搜索走**（与仪表盘同一个集合），超过 6 块合并成「其他」但**绝不丢**（丢了占比就加不到 100%）。<br>　· ⚠️ 订单账那一档要标「只含已取到的 N 笔」：合计与图都是拿**这一页流水**在客户端算的（服务端上限 1000 条）。<br>　· ⚠️ 扇形配色在 `ui/theme/Color.kt::ChartPalette`，硬判据是**两两 RGB 距离 ≥60**（`PieChartMathTest` 钉着）：两块颜色接近就只能靠图例反查，图等于白画。<br>**2026-09-19 第二次重做：四类账一律「仪表盘」**（用户点名，**推翻了同一天的上一版**）：原话「你这样子改不对啊…**假如客户非常多司机非常多，那用户要是一个一个去选的话，那要有多麻烦啊**。其实我觉得像这个账本啊，**所有的账本你可以一个仪表盘的思维进行去构建**」。<br>· ⛔ **多选 chip 墙已删**（`AccountPicker` / `DriverPicker` / `SelectedSummary` / `DriverSelectedSummary` 全部删除，`selectedAccounts` / `selectedDrivers` 状态一起删）：那一版的交互是"先在一堆 chip 里把人一个个点出来、再看下面的列表"，账户一多，"找到我要点的那几个"比看账本身还花时间；而且 chip 上写的"名字 + 金额"与下面列表里**是同一份信息**，重复放两处必然出现"两边对不上"的困惑。红线 `_tools/qa/_check_user_search.py` 钉着它不许长回来。<br>· **仪表盘卡**（`KpiBlock`）：**钱单独一行**（最大最粗、橙色），下面一行小字「N 个账户 · 共 M 笔」；笔数取**服务端**的 `count`（全量），不是本地明细行数（那是分页的）。四类账**同一个 KPI 版式**（订单账也换成了它）；口径词跟着窗口走（「（本月）」/「（全部）」）。<br>· **一个搜索框**（`ui/common/Components.kt::SearchField`）：姓名 / 手机号 / **手机号后 4 位**；**收窄之后的合计就写在仪表盘上**（"这两家一共多少"这么看，不需要勾选），并把「全部 N 个账户合计 ¥X」一起说出来 —— 不说的话用户会拿筛出的那个数当总额去对账。搜索框**只有一处**（横跨整页、在最上面），只在「人」的账里出现（订单账那一堆流水里没有"人"可搜）；它**筛的是人**（姓名/手机号/后 4 位），所以选人栏跟着它收窄。<br>· **账户行**（`LedgerAccountRow`）：名字 + **手机号**（同名不同人唯一的分辨依据）+ 笔数/单数 + 金额；账号停用/已删除标「已停用」。**点一下就是进他的账**（第二层）—— 三类账同一条路，原来司机账「就地展开」、货主账「另取流水再展开」那两条弯路已删。<br>· ⛔ **司机账 / 货主账 / 批发商账是同一支代码**（一支 `LedgerAccountRow` + `KpiBlock` ＋侧边抽屉），而「这一类账叫什么/什么颜色/什么图标」收进 VM 的 `kindTitle`/`kindLabel`/`kindColor`/`kindIcon` 各一处：上一版把它们散在页面里三处 `when (vm.tab)`，改一处漏一处。<br>· **明细里那一单就地展开**：`ui/common/OrderPeek.kt`（派单员账本与货主账本**共用一份**）。原来点一行是**跳到订单详情页** —— 跳过去再回来，筛选/展开的账户/滚动位置全没了，连着核几笔要来回跳十几趟；跳转那条路仍然留着（展开块右上角「打开订单」，看照片/导航/司机备注还得进详情页）。<br>· ⚠️ 时间窗口一变就要**重取已选中的那个人的账**（`refreshForNewWindow` 一处写全）：只换上面那张账户列表、下面的明细还停在上一段，就会出现「账户行写着本月、明细是上个月」（两边都不报错）。司机那一层不用重取 —— 他的单跟在账户汇总响应里。<br>· ⚠️ 选中的那个人**跨日期档位保留**（改档位只换窗口、不把人清掉）：用户的动作是"选一个人 → 换时间看他这一段"，把选人清掉等于每换一次时间都要重新点一次。<br>· **「记一笔账」= 单独一页**（2026-09-20 第七轮）：`ui/dispatcher/LedgerCreateScreen.kt`（`Routes.LEDGER_CREATE`，从账本页「记账」按钮进；回来时 `vm.onEnter()` 重取一次）。账户那个 `AlertDialog` **已删**（同一次记账不留两份）。用户原话：「这个商品是**可以在现有的商品库进行选择的**……**未注册的话也可以直接填**，会自动帮他注册一个临时账户……**单价是可以改的**」。三个字段各归各家：**商品只能从商品库选**（复用 `ui/common/ProductPicker.kt`，`single = true` 单选）、**货主 = 选已注册的 or 直接填未注册的名字**（填了就是 `temp_shipper_name` 临时账户，与「代理下单」同口径）、**单价自动带出但可改**（专属价优先；⚠️ 用户手改之后**换货主不许把价拽走**，判据是 `autoPrice`；⚠️ 换成**未注册的名字 / 清空**也必须重算 —— 临时货主没有专属价，不重算就会停在上一家的专属价上，真机实测抓到过 13.2 vs 正确 14.8，三个入口共用 `repriceIfAuto()`）。<br>　· ⚠️ 提交**不传 `total`**（后端用 Decimal 算 `unit_price × quantity`）：客户端 Double 乘一遍会带出 `36.900000000000006` 这种尾数（"同一个数两处算法"）。<br>　· ⚠️ 名册搜索走**服务端** `?q=`（`/users` 一页 500；本地过滤会让第 501 个货主"不存在"→ 用户去填一个临时货主 → **同一个人两本账**）；截断位与"搜索失败"都必须如实说出来，不许拿空列表当"没有这个人"。<br>　· 红线 `_tools/qa/_check_ledger_manual_entry.py`（44 项）+ 反向验证 18 种注入；后端这一轮**一个字节没动**（`POST /ledger/entries` 本来就收 `product_id` 与二选一的货主）。 |
| 运费结算 | `FreightSettlementScreen.kt` / `FreightSettlementViewModel.kt`。**2026-09-19 改成两栏**（用户：「我们那个运费计费那个图标啊，他那个功能，那个界面不是很好啊，我们采用一个**商品管理的一个界面的思维**，就是左边它有个篮子，那个篮子全是对应的司机，我们点击司机就能看，然后右边就是他们的详情。然后详情的那个最上面是有一个**统计**…然后后面这下面的就是**明细**，比如说关于一些订单啊的**价格明细**」）。<br>· 原来是**一列卡片**（每人一张、各默认露 2 单）：本机 293 个司机时，想核某一个人的账得滚很久，滚动过程中别人的钱一直在眼前晃。<br>· 左栏 = `ui/common/Components.kt::MasterRail`（司机名 + 「¥860 · 21 单」）；选中态是白底 + 左侧**珊瑚橙**竖条（本页语义色 `Accent = 0xFFFF8A65`）。<br>· 右栏 = **上统计、下明细**：统计块给三个数，**每个都标口径** ——「本月司机应得」（`driver_pay.pay_for_order` 逐单累加，与账单/报表同源）、「N 单已完成 · 货主运费合计 ¥X」（**另一个数**，不能混）、「其中 按件/提成」（非零才显示）、「有 M 单还没定价」（橙色警示，运费待补）。<br>· 明细行仍是「应得（大字）+ 运费（小字）」两栏，与统计**同一来源**；**整行可点** → 这一单的原始订单（`onOpenOrder` → `Routes.orderDetail`，与司机端「我的账本」同一处落点）。<br>· 左栏顶部有**按人搜索**（`core/UserSearch`：姓名/手机号/后 4 位）——293 个司机没有搜索就是一列找不到人的名单。搜索框横跨整页（与商品管理/库存同一版式），不塞进左栏。<br>· ⚠️ 选中态存的是**字符串 key**（`d|<司机 id>`）而不是 DTO：换月份整批换掉 `data`，存对象会指向被替换掉的旧实例（界面亮着、数字是上个月的）。换月份**或换时间区间**后 `selectedKey` 清空、界面回落到第一位。<br>· ⚠️ 页顶那行「本月共 N 位司机 · 应得合计 ¥X」**不是装饰**：两栏之后"整个月一共要付多少"没有别的地方能看到（原来它在列表顶上那张卡上）。<br>· **时间窗口两种**（2026-09-20 用户：「他除了上个月上上个月，他还可以选择时间进行查看的」）：三个月份药丸（上上月/上月/本月，走 `?month=`）+ 第四格「自定义」（走 `?from=&to=`，弹层是**通用那一份** `ui/common/Components.kt::DateRangeDialog`，与订单/账本/库存筛选同源）。两条路打的是**同一个后端接口**、同一套口径（已送达、已计价、按送达时间落窗口、隔离区不算）。<br>· ⚠️ **页面上的口径词必须跟着窗口走**：`periodLabel`（标题/药丸，自定义时直接显示 `09-01~09-20` 那段日期）+ `periodWord`（句子里用：「本月」/「所选区间」共 N 位司机、司机应得、空列表文案）。自定义区间时还写"本月"就是在说假话，而数字本身没法自证是哪一段的。<br>· ⚠️ 自定义起止按**当地整天**取（`"$from 00:00:00"` ~ `"$to 23:59:59"`）：不补时间的话"选到今天"会把今天送达的单整批漏掉，而界面上看不出少了一天。 |
| **界面文案**（几个字 + 解释只出现三次） | `core/HintPrefs.kt`（SharedPreferences 计数，默认 3 次；`alwaysOn` = 「提示一直显示」）＋ `ui/common/Components.kt::HintOnce`（解释性文案的唯一渲染入口）＋ `ui/profile/ProfileScreen.kt`（「我的 → 提示」开关，默认**关**） | 各页面常驻的短标签；解释性长句的调用点见 `OrderDetailScreen`（补导航块 / 补导航弹层 / 位置图片）、`OrderCreateScreen`（`ConfirmActionDialog` 的三个共享库动作） |
| **账本流水的来源名** | `core/LedgerSourceLabel.kt::ledgerSourceLabel`（**唯一一份实现**） | AI 记账/改账卡片（`ai/AiWrite.kt::AiLedgerRef.sourceLabel` 委托给它）＋ 派单员账本扇形图的图例。各写一份 `when` 的后果不是崩，而是**同一个科目在两张界面上叫两个名字**（「退款」 vs 「货损红冲」），用户只能当成两回事 |
| **图表**（折线 / 条形 / 扇形 + 配色） | `ui/common/Charts.kt`（三种图的唯一实现 + 纯函数 `pieAngles` / `percentText`）＋ `ui/theme/Color.kt::ChartPalette`（两两 RGB 距离 ≥60，`PieChartMathTest` 钉着） | 派单员账本用；⛔ 别处**不许自己 `Canvas(` 画图**（红线 `_tools/qa/_check_ledger_dashboard.py` 会红，白名单只有 `util/Watermark.kt`＝导出水印）。取数一律走各页自己的纯函数（账本是 `ui/dispatcher/LedgerCharts.kt`） |
| **日期档位**（全部/今天/昨天/前天/近 7 天/上周/本月/上月） | `ui/common/DatePresets.kt`（`rangeOf(label, today)` 纯函数，**唯一一份区间算法**，有单测）＋ `ui/common/Components.kt::DatePresetRow`（胶囊那一行，唯一一份） | 通用筛选条 `DateRangeFilter`（订单/账本/库存流水）与派单员账本共用。⚠️ **本月 = 1 日~今天、上周 = 上周一~上周日**；各写一份就会出现"两处本月差几天"。⚠️ 报表中心那套「按日/周/月」时间导航是**另一件事**（翻到哪一天），不在这一份里 |
| 运费模板 | `FreightTemplatesScreen.kt` / `FreightTemplatesViewModel.kt` || 挂账单位 | `ArrearsUnitsScreen.kt` / `ArrearsUnitsViewModel.kt` |
| 库存管理 | `InventoryScreen.kt` / `InventoryViewModel.kt`。**版式与商品管理一致：左边分类、右边商品，另加按名称搜索**（用户 2026-09-19：「库存管理也是跟商品管理一样的，左边是分类右边是商品，并且可以通过搜索名称来搜索商品」）—— 判据与导航条复用 `ProductPicker` 那一份（`CategoryRail` / `categoryTabsOf` / `categoryNameOf`），顺序取 `GET /product-categories`。搜索框横跨整页（在分类条**上面**）：分类是"这一块有哪些商品"、搜索是"那个商品在哪"，放右栏会被压成半宽、而且分类为空时它会跟着消失 —— 而"找不到某件商品"正是最需要它的时候。<br>⚠️ 卡片是**两行**（名称+库存 / 出入库按钮另起一行）—— 右栏少了 92dp，按钮再横着排会把商品名和库存数字挤成一堆省略号。<br>**出入库两个按钮（用户 2026-09-19 第二次调整）**：「那 2 个按钮换一下位置，且不要**贴**得太紧了…那个左边那个名字也**放**在下面，也就库存的下面，这样子做个区分」→ **出库在左、入库在右**（原来入库在左）＋**各占一半宽度、中间留 14dp**（原来只有 8dp 且都是内容宽度，两个 2 字按钮几乎粘在一起 —— 而出库点错成入库是**直接改库存**的错，不是视觉问题）＋ 整组仍在**库存那一行下面**（数字在上一行、动作在这一行，两件事分开） |
| 商品管理 | `ProductsScreen.kt` / `ProductsViewModel.kt`。**版式 = 左边分类、右边商品**（用户 2026-09-19：「这个商品管理也做成选择商品的那种界面，左边是分类右边是商品」）—— 分类导航条与"商品属于哪一类"的判据都复用选品页那一份（`ui/common/ProductPicker.kt` 的 `CategoryRail` / `categoryTabsOf` / `categoryNameOf` / `categoryOf`），**不许各写一份**（各写一份会出现"同一件商品在选品页属于日化、在商品管理页属于未分类"）。<br>**搜索框**（用户 2026-09-19：「商品管理的页面要有个搜索的框啊，方便我们找商品」）：横跨整页、在分类条上面，与**库存管理页同一版式**；本地过滤 + 与左边分类 **AND**（商品名册几百条，本地过滤比每次打字都打后端快）；搜不到时那句要说清是"搜不到"还是"这一类是空的"。<br>**底部导航栏**（用户 2026-09-19：「给那个商品管理界面的下面加个导航栏，左边分组管理、右边商品新增，那个 + 把它改成商品新增…两个导航栏做得美观一点」）：左「分类管理」描边次按钮 + 右「商品新增」实底主按钮（`PrimaryActionButton`，含白图标），都染**商品管理语义色紫** `ProductPurple`；⛔ 顶栏那个「分类管理」文字按钮与右下角 FAB **都已删掉**（两个动作各占一个角、视线要跑两趟，FAB 还压着列表最后一张卡 → 列表尾巴上那句 `Spacer(72.dp)` 也跟着删了）。⛔ 主按钮**不许**用钱的橙（同屏「售价」已是橙的）。<br>**商品卡上只有两个数字**（售价橙 + 库存蓝青；**成本与分类都不在卡上**，**一个数字一行**，库存还按状态变色）—— 见 §2.3 上一行与 `06_DESIGN_SYSTEM.md §4`，改之前先读那几条理由；动作（各批发商价格/上架下架/编辑/删除）在卡片右上角 `⋮` 菜单里，**它下面还有一个「改价」快捷入口**（**紫色**，只改默认售价，用户 2026-09-19 要求：「下面比较空嘛，在下面再加一个改价…方便、快捷」；⛔ 颜色**不许**用钱的橙，会和旁边的售价撞色），**不许**把动作再摊成三个按钮（用户：「太占位置了」）。<br>⚠️ 快捷改价的弹窗预填价要用 `util/Money.kt::trimMoneyZeros`（去尾零、不丢精度），**不是** `formatMoney`（两位小数，会把 12.3456 显示成 12.35 = 骗人）。<br>⛔ 商品编辑页**没有"批发价档位"**了（2026-09-19 删）：那个概念看着像批发价、下单时谁都不照它走，批发商的价格只有「各批发商价格 / 定价」两个入口设 |
| 批量改价 | `BatchPriceSheets.kt`（`mode` 只有**统一单价 / 按售价%**两档 —— 原来的"引用批发价档"随 `tier_prices` 一起删了） |
| **价格矩阵（批发商专属价）** | `PriceMatrixScreen.kt` / `PriceMatrixViewModel.kt` —— **一个商品 × 一个批发商 = 一个价**，**两个方向共用这一页**：<br> · `PriceAxis.BY_SHIPPER`：入口 = 批发商管理 →「定价」，行 = 商品（这个批发商买每个商品多少钱）；<br> · `PriceAxis.BY_PRODUCT`：入口 = 商品管理 → 卡片 `⋮` →「各批发商价格」，行 = 批发商（这个商品卖给每家多少钱）。<br>⚠️ 两个方向是**同一件事**（设那个（批发商 × 商品）的价），所以交互刻意做成一样的（用户 2026-09-19：「保证操作与逻辑匹配，并且是那种不是很复杂的操作」）。<br>⚠️ 取数一律**服务端筛**（`?shipper_id=` / `?product_id=`），不许拉全表再 filter。<br>⚠️ `BY_PRODUCT` 只列**启用中**的批发商，并把"另有 N 个已停用/已删除的没列出"写在页面上（给人设价是白设：账号软删后 `_del{id}`，人已经登不进来也下不了单）。<br>⚠️ 保存后要**清该行草稿**、批量调价后清**全部**草稿：`fieldValue` 是"草稿优先"，不清的话界面会拿你上次敲的数盖住库里刚写进去的新价（界面上看不出来） |
| 司机/货主/批发商用户管理 | `UsersManageScreen.kt` / `UsersManageViewModel.kt`（**司机池多一条「车辆」线**：车队摘要 + 搜索 + 每张卡一行「他开哪辆车」，点它配车/换车/解绑 —— 车辆行与绑定的口径见 §1「车辆 / 运费模板」那一行）。<br>**2026-09-19：搜索升级成「三个池都有 + 服务端搜」**（用户：「还有其他的比如说，**司机管理**啊**账户管理**啊。这些也要添加搜索键…根据他们的**名称**还有**电话号码**以及**电话号码的后 4 位**进行搜索」）：① 货主池/批发商池**以前没有搜索框**（只有司机池有），现在三个池都有；② 搜索打到**服务端** `?q=`（`app/core/user_search.py`）—— 一页上限 500，本地过滤时第 501 个人在客户端**根本不存在**，而"搜不到"会被读成"没有这个账号"；③ 结果**不覆盖名册**（`hits` 与 `users` 分开：`driverNameOf` / 车队摘要 / 配车弹层要完整名册，覆盖了会出现「账号 #5（不在司机名册里）」这种假故障）；④ 搜到 0 条时的说法从"这一页里没有"改成**结论**（服务端搜过了，确实没有 → 可以放心新建）。<br>⚠️ **车牌不再参与这个搜索框**（它原来只在已列出的那一页里匹配，同样有上限问题）—— 车牌优先的入口是右上角「车辆」→ 车辆管理页（那里有专门的「搜车牌」） |
| 车辆管理（v3.44） | `VehicleManageScreen.kt`（列表 + 编辑弹层 + 绑司机 + 停用；原来是 `AccountToolsScreens.kt` 里一个"只能新增"的车辆台账）。<br>⚠️ 弹层里的**司机候选搜索**走 `core/UserSearch`（唯一一份规则）；原来那里就地写了第 3 种口径（姓名 or 手机号 or **用户名**），与后端 `?q=` 和名册页各不一样 —— 同一件事三个答案，用户只会觉得"有时搜得到、有时搜不到" |
| 账号管理 | `AccountManageScreen.kt` / `AccountManageViewModel.kt`、`AccountToolsScreens.kt`（收款/结算/开销三屏，车辆已迁出）。**2026-09-19 加搜索框 + 服务端 `?q=`**：这一页列的是**全部角色**的账号（一页上限 500），原来**一个搜索框都没有**，而右下角就是「新建账户」—— "列表里没有"在这里必然被读成"这个账号不存在" |
| **共享地点库**（**全库共用**的一张表） | `backend/app/api/v1/places.py`、`backend/app/services/place_service.py`（**距离判据与"名字地址不能都空"的唯一一处实现**）、`backend/app/schemas/place.py` | `app/api/v1/shipper.py::share_location`（设为共享）、`android/.../ui/shipper/OrderCreateScreen.kt::AddressPickerSheet`（行尾 `⋮`）、回归测试 `backend/tests/test_place_manage.py`（10 例） | 用户 2026-09-19：「这个地址是可以编辑的。如果是来到了共享库的话，共享地址的编辑**只有派单员**可以编辑，其他人都编辑不了。派单员可以**改名称**，也可以把一些地点给**设置为共享地址**，也可以**撤销**某些共享地址，把它**降为普通的地址**，或者直接**删掉**。如果是降为普通的地址的话，则这个地址会保存在**派单员**的地址库当中，其他的不会显示」。<br>· **四个动作**：`PATCH /places/{id}`（改名称/地址，**坐标刻意不给改**——那是"某个位置在哪儿"的事实，指错了司机照着走就是错的）、`DELETE /places/{id}`（**软删**：`is_deleted` + `deleted_at`，`POST /places/{id}/restore` 原样放回）、`POST /places/{id}/demote`（撤销 → 存进**操作人自己**的「我的地点」+ 从共享库撤下）、`POST /shipper/locations/{id}/share`（设为共享：输入是**地点编号**，坐标从那一条上取）。<br>· ⛔ **权限一律 `require_roles(DISPATCHER)`**：`POST /places`（记录一个点）三种角色都能走——司机在现场、货主在门口，谁有坐标谁补；而改/撤/删动的是**别人也在用**的那条记录，所以要一个人负责。<br>· ⚠️ **删除是软删**（2026-09-19 用户定的硬规矩：「所有功能的删（撤）销操作就是软删啊，他们都是要有的」；第一版写成物理删除的理由已作废，见 `place_service.delete_place` 的注释）。三处判据必须一起认标记：列表/详情、**`find_place_near` 的合并判据**（漏这处的后果最隐蔽：新点会并进一条谁也看不见的行，两边都不报错）、restore 只放回被删那一条。`place_user_usage` **不用跟着删**了（软删白捡的好处；恢复后"用过几次"照旧）。<br>· **AI 也有一套**（用户点名：「再给派单端的 AI 去增加这些功能」）：写能力域「共享地点」4 个动作 `place.update/delete/demote/publish`（不进 `SHIPPER_ACTIONS` ⇒ 货主默认没有）。⛔ 模型**全程碰不到经纬度**：`POST /places` 仍然在 `_write_coverage` 的"永久排除"里，而"设为共享"的输入是**地点编号**。<br>· 撤回：`place.update` 有（写回旧值）；`place.delete` 有（`PLACE_RESTORE` 配对，撤回卡拿着确定编号）；撤销/发布没有，理由写在 `AiRevert.none(...)`。 |
| **到仓自动入库**（**独立的一条库存线**） | `app/services/warehouse.py`（`WAREHOUSE_METERS=60.0` / `warehouse_for_order` / `auto_warehouse_inbound`）、钩子在 `app/services/order_flow.py::complete_delivery`（紧跟 `auto_stock_commit` 之后） | `app/api/v1/shipper.py`（标仓库：**只有派单员**）、`InventoryMovement.source="WAREHOUSE"`、回归测试 `backend/tests/test_warehouse_inbound.py`（4 例） | 用户 2026-09-19：「有一趟货是到这个仓库，**刚好这个货物商品管理里面是有的**，这时候我们就自动地入库」、「你就再**开一条计算线**，这条线是**独立算的**…这个入库就正常入；直到那个订单完成了之后它才会减库存」。<br>⛔ **边界（改之前先读）**：它**只加不减、也绝不动"预占"** —— 订单自己那条线（派单预占 → 送达实扣）照旧由 `inventory_service` 负责，两条线各算各的。**不要**在这里调 `auto_stock_commit/release`，也不要因为"这一单是入库的"就少扣一件。<br>⚠️ **净效果要说清**：这一单的商品行若也能解析出商品，送达会**同时**产生 −N（订单实扣）与 +N（到仓入库），账面上两条流水都在；"库存要真的增加"属于**另一条决策**（让这类单不走实扣），没有默认打开。<br>⛔ **商品找不到的行跳过并报出来**（`skipped` → 操作日志 `warehouse_inbound_skipped`）：`_resolve_product` 是"有编号用编号、没编号则**同名且唯一**才认"的唯一实现，**不许**另写按名字模糊匹配的兜底。<br>⚠️ 识别半径是 **60 米**（不是 `place_service.MERGE_METERS=1.0`：那个是"这俩是不是同一个地点"的**合并**半径）；坐标与地址原文两条判据都是确定性的。<br>⚠️ 幂等：同一单只入一次（判据 = 已有 `source="WAREHOUSE"` 的流水）。 |
| **地点分组管理** | `PlaceCategoriesScreen.kt`（**面板，不是一页**） / `PlaceCategoriesViewModel.kt`。**按人分区**（货主和派单员各管自己那一份，互相看不到）。入口只有一条：下单页地址库左栏**底部那格「管理分组」**（用户圈着截图说「就在这个红框的位置，它跟左边那一个分组类别是一个**对齐**的状态」）。<br>· **它是"抽屉里的第二层"**：点它时**在同一个 ModalBottomSheet 里换内容**，不是 dismiss 再 push 一整页。第一版做成了独立路由（`Routes.PLACE_CATEGORIES`），真机上用户看到「地址抽屉先收起来、再弹一整页」中间闪一下，原话是「这样太麻烦了…干脆就直接弹一个，也算一个抽屉吧，**它 2 个抽屉**」→ 路由与整页已**删除**。<br>· 建（顶部输入框 + 新建）、改名（弹窗，**级联改掉挂着的地点**）、删（**还有地点挂着时后端拒绝**）、**排序两种方式共用一处逻辑**：位次输入框（填第几位，与「商品分类管理」同一个交互）+ 上下箭头，两者都提交**整份顺序**。搬运交给 `ProductCategoriesViewModel.kt::moveItemTo`（`moveCategoryTo` 的泛化版，**一份实现**，用户要求"直接复用商品管理那套"）。<br>· ⚠️ 位次显示用**列表下标**，不是 `sortOrder + 1`（后端新建给 `max+1`，第一个是 1）—— 真机抓到显示成 2。<br>· ⚠️ 长按拖动的手势**没有**搬过来（分组通常只有几个，数字框够用；拖动那套在抽屉里还要处理滚动冲突）。<br>· ⚠️ **改名会级联改掉挂着地点的 `category`**（后端做的），所以"只刷名册"会造出两个假象：① 抽屉里点那个刚改名的分组 → 「这个分组下还没有地点」（本地 `locations` 还带旧名字，一条都筛不到）；② 左栏选中项被删/被改名之后指向一个不存在的 key → 右栏空白。所以① 抽屉打开时 `reloadAddressLibrary()` **三份一起刷**，② `OrderCreateScreen` 里 `LaunchedEffect(categories)` 核对一次选中项、对不上就回落到「我的地点」。<br>· 分组是**可选**的：不建分组，地址库照样用默认那三段。<br>· **AI 也能做这一套**（2026-09-19 用户要求）：写能力域「地点分组」4 个动作（`place_category.create/update/delete/reorder`）；另外 `location.update` 多了 `category` 字段（「把这个地点归到那一类」）。⛔ 归类**严格对名册**：名字对不上就拒绝并列候选，**不顺手新建**（错别字会静默多出一格分组）。⚠️ 货主**能用**这 4 个动作（他自己地址库的事），所以它们在 `AiWrites.SHIPPER_ACTIONS` 里；商品分类不在（那是全店一份） |
| 商品分类名册 | `ProductCategoriesScreen.kt` / `ProductCategoriesViewModel.kt` |
| **报表中心入口页** | `ReportHome.kt`（2×2 彩色图标卡） |
| 报表详情 | `ReportCenter.kt` / `ReportCenterViewModel.kt`、`ReportScreens.kt`、`ReportViewModels.kt` |

### 2.4 货主 / 司机

| 端 | 功能 | 文件 |
|---|---|---|
| 货主 | 下单 | `ui/shipper/OrderCreateScreen.kt` / `OrderCreateViewModel.kt`（**2026-09-19：地址库抽屉改成两栏** —— 用户：「我们也改成类似商品管理的形式，因为我们有 **3 个分组**嘛，3 个分组就放在左侧，然后右边就是对应的地点」。原来三枚横向胶囊（`SegmentedStatusTabs`）只能显示"现在在哪一段"，两栏能同时看到三段 + 自定义分组 + 底部那格「管理分组」（**一格都不带「N 条」**，见设计规范 §4.6）。左栏同样是 `MasterRail`（无副标题 → 行高自动 52dp，选中竖条用主题主色）；搜索框仍在左栏**上面**横跨整页，**三段共用**，`if (sel == "p") onSearchPlaces(...)` 那条"共享地点顺带打后端"的线保持不动（红线 `_check_ai_guardrails.py` §31 钉着）<br>· **长标题从尾部省略**（`SheetRow` 的标题用 `TextOverflow.StartEllipsis`，并独占剩余宽度）—— 见设计规范 §4.7，用户原话「省略是可以的，但是你要**从后面往前显示**」<br>· ⚠️ **打开抽屉时刷三份**（`OrderCreateViewModel.reloadAddressLibrary()`：分组名册 / 线路 / 我的地点）：VM 挂在路由上随页面复用、这四份数据只在 `init` 拉过一次，不刷的话"刚在地址与联系人建的地点这里选不到"，而分组**改名**在后端会级联改掉地点的 `category`，只刷名册会出现"点进去说这个分组是空的"<br>· **共享地点那一行不带「司机补录 · 用过 2 次」**（用户 2026-09-19 点名）：选地址时"谁标的、别人用过几次"都帮不上忙<br>· **派单员**在行尾看到 `⋮`（`SheetRow(actions = …)`）：共享地点 → 改名称或地址 / 撤销为我的地点 / 从共享库删除；我的地点 → 设为共享地址。货主**连那个按钮都不画**（`canManageSharedPlaces`，判据是登录角色，真门在后端 `require_roles(DISPATCHER)`） |
| 货主 | 我的订单 | `ui/shipper/ShipperOrdersScreen.kt` / `ShipperOrdersViewModel.kt` |
| 货主 | 地址与联系人 | `ui/shipper/AddressScreen.kt` / `AddressViewModel.kt` |
| 货主 | 账本（**2026-09-20 第四轮改成"以订单为基础"**） | `ui/shipper/ShipperLedgerScreen.kt` / `ShipperLedgerViewModel.kt` + **纯函数** `ui/shipper/ShipperLedgerGrouping.kt`（有单测 `ShipperLedgerGroupingTest`）。<br>**两种货主、两种能力**（用户原话：「分两种货主，一个是普通货主、一个是批发商货主」）：<br>· **普通货主**：搜索订单 + 日期档位（**按送达日**）+ 自己的订单列表（点进详情）+ 一个「欠总分销商」合计。**没有核销** —— 他给自己下单，没有第二个债务人；<br>· **批发商货主**（`users.is_member=1`，App 侧由 `repo.me().isMember` 判）多一段：`① 我欠总分销商多少` + `② 我的货主（联系人）欠我多少`（**先联系人总计、再他名下的订单明细**），每单可就地**核销**（整单 / **按商品行**）、可**撤销**（软删，可恢复），界面上还有「已撤销」区可恢复；<br>· ⛔ **两本账谁也不写谁**：批发商那本走后端 `api/v1/shipper_ledger.py`（新表 `shipper_settlements`），**一个字节都不写** `orders.paid` / `cash_flows` / `ledgers`；<br>· 归属人取**订单上的收货人名称**（真实数据里 108 张批发商订单的**下单人恒为批发商自己**、收货人才是各不相同的客户），空则退到下单人，再空进「未指定货主」；<br>· 旧「账目流水」（`/ledger/entries`，手动记账/货损红冲只在这里看得到）保留成顶栏二级入口，没删能力。<br>· **两个视图共用一个时间窗口**（2026-09-20 22:5x，用户第三次点名：「你这个账目流水为什么也不做对应的那个右上角的时间图标？他还是那个滑动型的」）：顶栏药丸切视图**不消失**、流水页里**不再有** `DatePresetRow`、换档走 `reloadWindow()` **两边一起重取**（只刷一个的话切回去会看到上一个窗口的数）。依据是账本行的 `entry_date` 本来就等于**订单送达日**（`services/ledger_sync.py:27`；`api/v1/orders.py:204` 的注释也写明两边口径对齐）。<br>· ⚠️ **`init` 里必须先 `switchPreset(今天)` 再取数**（不能直接 `load()`）：`preset` 初值是「今天」而 `rangeFrom/rangeTo` 初值是 **null＝不带日期条件**，直接取数会先拉一整份"全部"回来（药丸写今天、屏幕是全部，实测 logcat 抓到 `GET /orders?limit=500` 无日期在前） |
| 司机 | 任务列表 | `ui/driver/DriverOrdersScreen.kt` / `DriverOrdersViewModel.kt`（**2026-09-20：日期窗口 = 顶栏右上角一个药丸** —— `DatePresetPill` + 点开 `DatePresetDialog`（全部预设 + 自定义）。原来那行 **9 个胶囊太长、要横向滑动、右边还会被切掉**，用户点名换成这个形态（参考账本页右上角）<br>· 顶栏按用户点名：**左＝刷新、右＝时间药丸**（药丸只在「已完成」出现）<br>· **默认档位 = 今天**，且**第一次进「已完成」会自动退档**到真有单的那一档（`pickDefaultPreset`，阶梯见 `DriverOrdersViewModel.DEFAULT_LADDER`：今天→昨天→前天→这周→上周→近 7 天→本月→上月→全部）—— 用户 2026-09-20：「默认是看今天的，然后其次再往上推昨天、前天、这周，然后上周依次类推」<br>· ⛔ 退档**只跑一次**：用户自己点过药丸（`userPickedPreset`）之后永不再自动改<br>· ⚠️ 药丸与空态**都不许藏在"列表非空"的分支里**：默认档位是今天，今天没单时列表本来就是空的 —— 藏起来用户就换不了档（这就是用户报的"点今天什么都拿不到"）<br>· 日期换算只有 `DatePresets.rangeOf` 一份；`preset`/`customFrom`/`customTo` **必须声明在 `init` 之前**（红线 `_check_vm_state_before_init.py`） |
| 司机 | 运费（「我的账本」） | `ui/driver/DriverFreightScreen.kt` / `DriverFreightViewModel.kt`（**2026-09-20：时间控件 = 顶栏右上角药丸**（`DatePresetPill` + `DatePresetDialog` + 自定义走 `DateRangeDialog`），与司机任务页同一个控件。原来那行是按日/周/月翻页的 `ReportTimeNav` —— **那个组件没删**，报表中心（`ReportCenter.kt`）还在用<br>· **默认档位 = 今天**，首次进页面**自动退档**到真有单的那一档（`pickDefaultPreset`，阶梯与司机任务页**共用** `DRIVER_PRESET_LADDER`）<br>· **趋势图 X 轴粒度跟着窗口走**：单日档（`periodStart == periodEnd`）→ 按**小时**（标签 `20时`），跨日档 → 按**天**（`09/15`）<br>· ⚠️ **「自定义」档必须在 `windowOf()` 里接上 `customFrom/customTo`**：`DatePresets.rangeOf` 对自定义返回 null，漏接的后果是"药丸写 `09-15~09-15`、请求却发全部"（实测踩过）<br>· ⚠️ **时间显示必须走 `util/TimeFmt.formatDateTime`**（naive 串按 UTC 解释再换算到设备时区）。直接 `take(16)` 印的是 **UTC**，而这条接口的窗口按**当地日**（`BUSINESS_TZ = 东八区`）算 —— 会出现"明细写 09-15 18:18、筛 09-15 却查不到"（它当地是 09-16 02:18），且模拟器时区恰是 UTC，**这个错只在真机上看得见**<br>· 明细卡片：主行 = **地址**（有起点时「起点 → 终点」；`orders` 表没有起点列，后端暂不出 → 现在只显示终点）、次行 = 时间 · 配送说明、订单号降为小字（用户：「订单号都不是非常重要」，点进详情可看）<br>· ⛔ 合计与趋势图**只能用 `payTotal`（司机应得）**，口径只有 `services/driver_pay.py` 一处 |

### 2.5 通用 UI 与工具

| 功能 | 文件 |
|---|---|
| **输入框的数字/格式过滤（电话 / 金额 / 数量）** | `core/InputRules.kt`（**唯一实现**，纯 Kotlin、有单测）—— 完整规则、后端那一半与两条例外见 §1.5 |
| **应用图标（桌面图标 / 主题图标）** | `res/drawable/ic_launcher_background.xml`（浅蓝 `#D9E8FF`）、`res/drawable/ic_launcher_foreground.xml`（登录页那个货车 `LocalShipping`，深藏蓝 `#0A3168`）、`res/drawable/ic_launcher_monochrome.xml`（Android 13+ 主题图标）；自适应图标声明在 `res/mipmap-anydpi-v26/ic_launcher{,_round}.xml`。**三层必须一起改**（只改一层会出现"彩色版是货车、主题图标是别的形状"） |
| 通用组件 | `ui/common/Components.kt` |
| **订单状态门（哪一档能做哪个动作）** | `core/OrderStatusModel.kt`（`ALL`/`CANCELLABLE`/`RECALLABLE`/`ASSIGNABLE`/`LINE_EDITABLE`/`ACKABLE`/`COMPLETABLE`/`EDITABLE`/`FREIGHT_EDITABLE`/`NOT_CANCELLED`/`DRIVER_OPEN`） |
| 订单卡片 | `ui/common/OrderCard.kt`（**收货人 / 下单人两行**：名称与电话由 `contactWho` 拼，卡片与详情共用这一份 —— 见上面「收货人与下单人」那一行）<br>· **多个商品之间一条虚线**（`DashedLine()`，用户 2026-09-20：「卡片的多个商品……中间做虚线横杠稍微做一个区分，省的看错位」）—— 名字在左、数量在右，两行紧挨着排很容易把「×3 / ×4」看成同一行的<br>· ⚠️ 那条虚线是**纯布局拼的**（一串小 `Box`），**不许改用 `Canvas`**：红线「自己画的图只许在 Charts.kt」（`_check_ledger_dashboard.py`）会当场红<br>· 司机视图的金额门控见 `backend/app/services/order_response.py::apply_driver_view_gating`（`freight_visible = mode == "PIECE"`；固定工资司机连 `freight_fee` 都是 `None`） |
| 状态分段导航（统一规范） | `ui/common/SegmentedStatusTabs.kt` |
| 图表 / 报表时间导航 | `ui/common/Charts.kt` / `ReportTimeNav.kt` |
| 高德选点 | `ui/common/AmapPicker.kt` |
| ViewModel 工厂 | `ui/common/ViewModelFactory.kt` |
| 金额格式化 | `util/Money.kt`（统一两位小数 `formatMoney`） |
| 时间格式化 | `util/TimeFmt.kt` |
| 高德 URI / 地理解析 | `util/AmapUri.kt` / `util/GeoResolver.kt` |
| 导出 | `util/ExportUtil.kt` |
| 水印 / URL 解析 | `util/Watermark.kt` / `util/UrlResolver.kt` |

### 2.6 打包 / 发布 / 应用内更新（**跨 6 个文件的一条链，别只改一处**）

| 功能 | 核心文件 | 附带文件 / 注意 |
|---|---|---|
| **应用内更新（「检查更新」）** | `ui/profile/ProfileViewModel.kt`（下载 + 调安装器）、`ui/profile/ProfileScreen.kt`（进度与引导弹窗） | ⚠️ **必须同时看 `res/xml/file_paths.xml`**：FileProvider 只暴露那里声明的根目录，APK 落盘目录不在其中就会抛 `IllegalArgumentException` 被吞掉，现象是"进度 100% 后毫无反应"（2026-09-15 真事故） |
| 下载进度文案（速度/剩余时间） | `ui/profile/UpdateProgress.kt` | 纯函数无 Android 依赖 → 有 JVM 单测 `UpdateProgressTest.kt` |
| 版本声明（服务端） | 服务器 `ackend/uploads/app/version.json` | **必须含 `versionCode`**；App 按它判新旧（版本名字符串比大小会错） |
| 打包配置 | `android/app/build.gradle.kts` | ABI flavor（`phone`/`emu`）、versionCode 方案。**改动前先读 `docs/APP_UPDATE_AND_RELEASE.md`** |
| 推送脚本 | `_tools/deploy/publish_apk.py` | 推之前比 versionCode，不更大就中止；推完回读验证 |
| 红线检查 | `_tools/deploy/_check_update_flow.py` | 31 项，把上面这些跨文件不变量固化；改坏立刻红 |
| 本地造"假新版本"验证 | `_tools/deploy/_stage_local_update.py` | 只有真跑一遍才知道 FileProvider / 安装权限 / 安装器对不对 |

> 完整背景与实测记录：[docs/APP_UPDATE_AND_RELEASE.md](../APP_UPDATE_AND_RELEASE.md)

---

## 3. 危险区（改之前务必确认）

1. **`backend/app/core/schema_bootstrap.py`** — 生产库曾因 `orders.status` / `ledgers.source` 枚举缺值导致 500。改枚举值必须**同时**改 `enums.py` 和这里的迁移。
2. **`order_flow.py::complete_delivery`** — 一个函数同时改状态 + 写货主账本 + 写司机应付 + 记货损。**动它会连带影响三张报表**。
3. **`app/api/v1/orders.py` L686（接单；这是「赋值行」，不是函数头）** — 这是**唯一一处不在 `order_flow.py` 的状态转移**（`driver_ack_view` 里 `order.status = OrderStatus.ACCEPTED`）。只翻 `order_flow.py` 就会漏掉"接单"这一步。函数 `driver_ack_view` 本体在 L675。
4. **库存联动** — 状态机每一步都调 `inventory_service` 的 `auto_stock_out`（派单预占，L82）/ `auto_stock_commit`（送达真扣，L202）/ `auto_stock_release`（撤销撤回，L155/L231/L255）。改订单流转**必须**同时确认库存语义。
5. **`order_flow.py::split_order`** — 数量按比例拆分、余数归首份，改动会影响对账。
6. **"账本写入幂等"只在正常路径上成立，且靠的是入口守卫——不是每个函数自己幂等。** 逐函数核实：

   | 函数 | 是否自身幂等 | 依据 |
   |---|---|---|
   | `ledger_sync.py::sync_ledger_from_delivered_order` | ✅ **是** | 按 `(order_product_id, source=ORDER)` 查已存在 → 走**更新**分支（L31-L50） |
   | `accounting_service.py::generate_piece_bill` | ✅ **是** | 按 `(order_id, bill_type=PIECE)` 查已存在 → 直接 `return existing`（L79-L86） |
   | `accounting_service.py::apply_damage_accounting` | ❌ **不是** | **无任何已存在检查**，对每行 `damage_quantity>0` 直接 `db.add(Expense)` + `db.add(CashFlow)` + `db.add(Ledger(source=REFUND))`（L104-L160） |

   **那为什么现在不会重复记账？** 因为唯一调用方 `order_flow.py::complete_delivery`（L219）在入口就卡了状态：
   `if order.status != OrderStatus.ACCEPTED: raise`（L182）——首次执行后状态已变 `DELIVERED`，第二次直接被拒。

   > ⚠️ **所以别把"重试安全"当成函数的性质**。`post_delivery_accounting` 是导出的公共函数，
   > 一旦有人**在别处再调一次**、或**放宽 L182 的状态守卫**（例如加个"补录货损"入口），
   > 货损就会**重复入账**（Expense / CashFlow / REFUND 红冲各多一条）。
   > 要动这块，先给 `apply_damage_accounting` 补一个"该订单是否已记过货损"的判定。
   >
   > 📌 **实测交叉印证**：[`99_STRESS_TEST_REPORT.md`](99_STRESS_TEST_REPORT.md) 记录过"双 `complete` → 一个 200 一个 400（状态机拦截），账本 1 条、司机账单 1 条，无重复入账"——
   > 与上面的静态分析一致。**但那次压测走的是无货损的普通完成**，只验了 `ledger_sync` 与 `generate_piece_bill` 两条幂等路径；
   > 唯一不幂等的 `apply_damage_accounting` **没被覆盖**。① 静态分析说它不幂等、② 实测没打到它——两者不矛盾，是**覆盖缺口**。
7. **JSON 列可能被存成 NULL**（`orders.image_urls`、`shipper_addresses.image_urls` 等），出参一律加 `field_validator(mode='before')` 归一为 `[]`。（`products.tier_prices` 是这条规矩最初的现场，但它 2026-09-19 已废弃、不再是出参字段。）
8. **`ui/nav/Modules.kt`** — 改底部导航会影响全部角色的入口，注意 Tab 索引越界（历史崩溃点）。
9. **图片白名单散布在 6 处（不是 2 处），MIME 与扩展名是两套不同的清单** — 改之前务必逐处核对：

   | 位置 | 清单类型 | 内容 | 谁在用 |
   |---|---|---|---|
   | `api/v1/products.py` L19 | **MIME（8 种，权威）** | jpeg/jpg/pjpeg/png/webp/x-png/bmp/x-ms-bmp | 商品图；**并被 2 处导入**（见下） |
   | `api/v1/products.py` L152 | 扩展名（5 种） | .jpg .jpeg .png .webp **.bmp** | 商品图 |
   | `api/v1/orders.py` L80 | MIME（**5 种，副本**） | jpeg/png/webp/jpg/pjpeg——**缺 bmp/x-png/x-ms-bmp** | **仅送达拍照**（L89） |
   | `api/v1/orders.py` L95 | 扩展名（**4 种**） | .jpg .jpeg .png .webp——**缺 .bmp** | 送达拍照 |
   | `api/v1/orders.py` L576 | — | **`from app.api.v1.products import ALLOWED_IMAGE_CT`（延迟导入权威版）** | 地址图（L589 扩展名 5 种） |
   | `api/v1/shipper.py` L220 | — | 同样**导入 products 的权威版** | 地点图（L233 扩展名 5 种） |

   ⚠️ 三个后果：① **送达拍照不接受 `.bmp`，但地址图/地点图接受**（因为后两者导入了权威版）——看似同类的上传入口，口径并不一致；
   ② `orders.py` L95 对未知扩展名**静默改名为 `.jpg`**（`products.py` L152 则是按 MIME 反推），所以该路径可能出现"扩展名是 .jpg、字节其实是 webp/png"的文件；
   ③ 别把 MIME 清单和扩展名清单当成同一份——它们是**两套**，数量也不同（8/5 与 5/4）。
   > 另注（2026-09-19 外部完整检查时核对，**上面这条旧说法已作废**）：`services/image_archive.py` 的 `_FORMAT_BY_SUFFIX` **包含** `.webp` —— webp 同样走「原地压缩」（重编码为 WebP q90 并按长边 1920 缩放），所以"webp 不进归档"不再成立；而 `.compressed.webp` 那类历史遗留的孤儿文件由 `purge_orphan_compressed` 直接删掉（顺带清中断留下的 `*.archiving`）。
10. **已知死代码（不要照着改）** — `services/cancelled_order_retention.py`（10 天常量与现行 30 天冲突）、`core/ws_hub.py`（零引用）、**`api/v1/orders.py` L546 `delete_order`**（与 L326 `delete_cancelled_order` 同方法同路径，FastAPI 先注册者胜 → 它**永远不可达**）。
    ⚠️ 第 3 个尤其阴险：它自己 L551 的注释写着"老路由…已被 307 行的软删除路由覆盖匹配，**保留为兜底**"——**两处都错**：行号过时（实际 326），且"兜底"不成立（FastAPI 不做 fallback，先匹配到的直接处理）。**读源码注释也会拿到错的结论。**
    > 这类"重复路由"是纯机械事实，已由 08A 自动列出（见其「需要注意的端点」第 1 节）——以后改端点后可重跑 08A 复查，不必靠人眼。

### 3.1 收款口径的隐性分裂（对账会打架）

全后端只有 **4 处**写 `CashFlow`，**全部在 `services/accounting_service.py`**（L126/L215/L337/L398）。

而订单流程里的"收现金"（`POST /orders/{id}/pay`、送达时选 cash）**只改 `orders.paid` 标记，不写资金流水**。唯一写 `CashFlow(IN)` 的客户收款路径是 `POST /ledger/receipts`。

**后果**：报表的「营业纵览 collected」（订单口径）与导出的「kind=finance 流入」（资金口径）**是两个口径，对账时必然出现差异**。改收退款逻辑前先确认你要动的是哪一套。

> 以上第 9、10 条与 3.1 由独立复核发现，机器校验（引用完整性）**查不出来**——它们都是"文件存在但语义不对"。

---

## 4. 常见「找不到文件」误区

| 你想找的 | 别去哪找 | 实际在 |
|---|---|---|
| 批发商定价模型 | `models/price_rule.py`（**不存在**） | `app/models/product.py` L49 |
| 订单状态定义 | `models/order.py` | `app/models/enums.py` |
| 真正的派单状态转移 | 只看 `order_flow.py`（**会漏掉"接单"**） | `app/services/order_flow.py` **5 处** + `app/api/v1/orders.py` **L686**（共 6 处） |
| 权限校验 | 各 endpoint 里逐行找 | [`08A_ENDPOINT_INDEX.md`](08A_ENDPOINT_INDEX.md)（机器生成的端点→授权表）。<br>⚠️ 单读 `app/core/rbac.py` **不够**：它是"角色→权限点"，且派单员对 `require_permission` 一律放行，推不出端点准入范围 |
| 定时任务 | celery / apscheduler（**都没有**） | `lifespan` + `services/data_retention.py` |
| Android 加新 Tab | 各处硬编码 | `ui/nav/Modules.kt`（**不在 `ui/common/`**） |

> ⚠️ **同名 / 单复数陷阱（后端最容易踩）**：有大量**跨层同名**文件——`ledger.py`、`shipper.py`、`inventory.py`、`arrears.py`、`order.py`、`reports.py`、`stats.py`、`auth.py`、`user.py`、`notification.py`、`freight_template.py`、`operation_log.py`、`export_job.py` 在 `app/api/v1/` 与 `app/models/` 各有一份，部分在 `app/schemas/` 还有第三份。
>
> 而且**单复数并不统一**——最典型的是商品：
>
> | 层 | 文件名 |
> |---|---|
> | 接口 | `app/api/v1/products.py`（**复数**） |
> | ORM 模型 | `app/models/product.py`（单数） |
> | 出入参 | `app/schemas/product.py`（单数） |
>
> 所以：**引用和搜索都要带层前缀 + 确认单复数**，否则会一次命中多份不相干的文件，或者搜了个不存在的名字。

---


> **客户端状态口径**：`python _tools/qa/_check_client_contract.py`（36 项：后端状态门 → H5/App 逐值对账、每档状态的入口、客户端不许出现枚举外的状态码、H5 调的端点是否真实存在、登出是否真调后端）／反向验证 `python _tools/qa/_reverse_verify_client_contract.py`（20 条注入）。
> **H5（`frontend/`）本轮起才进检查范围**：`npm install` 之后 `npm run build`（`vue-tsc -b` + vite）能跑，改 H5 后请至少跑一次它 —— 这个目录此前**没有任何自动步骤执行过**（这也是它带着「类型少一档」活下来的原因）。
## 4.5 挖漏洞 / 测试工具（`_tools/`）—— 想"换个角度测一遍"时先看这里

| 要测什么 | 工具 | 判据锚在哪 |
|---|---|---|
| 接口收到脏输入会怎样（崩溃/收下不该收的） | `fuzz/_fuzz_contract.py` | 5xx = 缺陷；英文 422 = 可疑；「明显不合理的值被接受」= 可疑 |
| 谁能调什么、能不能看别人的数据 | `fuzz/_fuzz_authz.py` | 源码 `collect()` 的授权声明 vs 实测状态码；跨租户只用自己建的单 |
| 库里的钱/库存/状态/单据还对不对得上 | `fuzz/_fuzz_invariants.py` | **直接只读查库**；司机账单用 `driver_pay.pay_for_order` 重算（钱只算一处） |
| 同一件事做两次会不会变成两笔 | `fuzz/_fuzz_replay.py` | 比较操作前后**库里的行数**，不看返回码 |
| 金额字段有没有上限（静态） | `qa/_audit_money_fields.py` | 判据 `import` 自 `backend/app/schemas/money.py`，不另抄一份 |
| **文本字段有没有上限**（静态） | `qa/_audit_text_fields.py` | 模型清单由 Pydantic 枚举；**目标表由字段/列名重合度算**，上限与列宽逐个比；扫不到字段就非零退出 |
| **「已付款却没有明细」的结算单** | `qa/_probe_settlement_orphan.py`（查，含各版本备份）、`qa/_repair_orphan_settlement.py`（修，带备份） | 只删"没有任何付款流水"的那种；有流水的留给人决定 |
| **探针账号的清理** | `qa/_cleanup_probe_users.py` | 判据 = `full_name like '%探针%'`；**软删**（`is_active=0` + 号码加 `_del{id}`，与 `DELETE /users/{id}` 同语义）+ 写操作日志 |
| 诊断：工具会碰哪些端点 / 按标记扫残留 | `fuzz/_list_ops.py`、`fuzz/_dbq.py` | 清单由 openapi 与库结构算出来（`_dbq.py --db` 真的会切库） |
| 收尾：清残留 / 修事故 | `fuzz/_cleanup_leftovers.py`、`fuzz/_repair_price_rules_batch.py` | 各自的判据与还原依据写在文件头 |
| 老探针（业务口径逐条问） | `qa/_probe_core_flows.py`（11 组；收尾会**软删自己建的账号**） | 见 §55 |
| **演示数据：清库 / 造数 / 验收 / 冒烟** | `_tools/seed/_reset_dev_db.py`（备份 + 清空业务表，保留 3 个开发登录）、`backend/scripts/seed_demo_data.py`（造近 3 个月规范数据，含**消息中心**）、`_tools/seed/_verify_demo_data.py`（**只读验收 63 项**：脏字 / 空字段 / 重号 / 批量复制 / 时间分布 / 钱对不对得上 / 照片在不在 / **派生日期跟不跟订单走** / **三个开发账号自己有没有数据** / **枚举词表** / **JSON 列形状** / 消息中心）、`_tools/seed/_smoke_endpoints.py`（**接口冒烟**：从机器生成的端点索引里取全部无参 GET，三端各打一遍，只认 5xx） | 用户 2026-09-20：「将现在所有的测试数据全部做一个清除…再重新写一份数据…**一定要正规**，不能是随随便便的，因为这次我们用来测试的数据很重要」。⛔ 造数**走真实业务函数**（`order_flow.assign_driver` / `complete_delivery` / `message_center.publish_*`，后者只把 Socket 推送换成空实现），账本/账单/现金流水/库存流水/消息由业务代码自己写；⛔ 派生记录的**日期**要跟订单走（漏一处就有一类数据全挤在今天且界面上看不出来，所以覆盖清单**自己算**：所有带 `order_id` 的表，未覆盖要写理由）；⛔ 收款必须走 `create_receipt`（直接改 `orders.paid` = 钱收了账上没有）；⛔ **验收用的是三个开发账号**（13800000001/2/3），它们必须各自有单/账/地址库；⛔ **词表只有后端那一套**：车型 `small/large/trailer`、开销分类 `fuel/…`、结算类型 `piece/salary` —— 写显示名（中文）、写大写，**写库都不报错，但读接口 500**，所以造完数必须跑一次冒烟；⛔ **JSON 列给 list、Text 列给字符串**（`delivery_photo_urls` vs `image_urls` 就是一对反例，给错会让 `GET /orders` 整个 500）；⛔ **id 与时间必须同向**（列表 `order_by(id desc)`，否则待派池第一张是两个月前的单） |

**三条工具纪律（每条都是被咬过才写下的，改工具前先读）**：
1. **批量/全量端点按类禁止**（`_fuzzlib.BULK_PATTERN`）——"接口只会改我自己建的数据"对批量端点不成立；
2. **看不见目标就非零退出**（`Report.guard`）——空转的绿灯比红灯危险；
3. **只动自己建的数据**——路径参数一律换成不存在的 id，并要求工具自检这条约束真的生效。

安全轨本身有反向验证：`python _tools/fuzz/_reverse_verify_fuzz_safety.py`（改坏一处必须报红）。

## 5. 维护约定（重要）

- **什么时候更新**：改动落在本表列出的任一「核心文件」时，同步检查对应行是否还准。
- **为什么必须更新**：**过期地图比没有地图更糟**——没有地图时 agent 会去读代码拿一手真相；有错地图时它会相信结论直接动手。
- **对齐标记**：本表依据分支 `p`、2026-09-14 的代码状态编写。大幅重构后请复核第 1、2 节的路径与行号。
- **行号会漂**：表里的 `L###` 是编写时的锚点。若行号对不上，用 `grep -n` 找函数名，不要因为行号变了就认为整条错了。
- **`L###` 的语义（曾写反过，已改正）**：本表的 `L###` 是**"这一处要去看的那一行"**，指向什么由上下文决定，共四种：
  1. **`def` 行** —— 引用端点时的**默认口径**（与 [08A](08A_ENDPOINT_INDEX.md) 一致，写 `L359 `create_order`` 这种"行号 + 符号名"的形式）
  2. **赋值/语句行** —— 危险区要的是"状态在第几行被改"，如 L686 是 `order.status = OrderStatus.ACCEPTED`（函数头在 L675）
  3. **常量定义行** —— 如 `data_retention.py` L28 `SOFT_DELETE_RETENTION_DAYS = 30`
  4. **`class` 行** —— 如 `models/product.py` L42 `class PriceRule`
  > ⚠️ 本表**曾经**写成"`L###` 指 `def`/`class` 行，不是装饰器行"——实测 14 个锚点里**一个 def 行都没有**（7 个装饰器行、3 个赋值行、4 个语句行），照那条约定读会全部跳错。**引端点时优先写「符号名」，行号只作提示。**
- **精确行号以 08A 为准**：[`08A_ENDPOINT_INDEX.md`](08A_ENDPOINT_INDEX.md) 由 `backend/scripts/gen_endpoint_index.py` 从代码生成，**每个端点的行号都是机器写的**（数量见该文件头部），且有 `--check` 守 freshness。本表的人工行号会漂，08A 不会。
  - 改完后端 API 后重生成：`cd backend && python -m scripts.gen_endpoint_index --out ../docs/PROJECT_MAP/08A_ENDPOINT_INDEX.md`
  - 校验有没有过期：加 `--check`，不一致返回 exit 1（可直接进 pre-commit）
- **改完文档，四件套各跑一次**（缺一件就会有盲区）：

  | 命令（在 `backend/` 下） | 查什么 |
  |---|---|
  | `python -m scripts.check_refs ..\docs\**\*.md` | 反引号里的路径是否真实存在（扫整个 docs 树） |
  | `python -m scripts.check_reachability` | markdown 链接是否有效 + **从 `AGENTS.md` 能否到达每一份文档**（报孤儿） |
  | `python -m scripts.gen_endpoint_index --check --out ..\docs\PROJECT_MAP\08A_ENDPOINT_INDEX.md` | 端点索引是否与代码同步 |
  | `python -m scripts.check_doc_claims` | 文档里的断言与计数是否与代码一致，**外加 markdown 结构**（表格列数、裸竖线、标题层级） |

  > **为什么不能只跑第一件**：实测一份 168 处引用的文档，`check_refs` 报 **0 处失效**，独立复核却找出 **9 处语义错误**。
  > 而且它**只认反引号**——`[文本](路径)` 形式的链接它根本看不见，而入口文件恰好全是这种写法。
  > **路径对 ≠ 内容对 ≠ 找得到。**
- **行数口径**：本表行数 = **物理总行数**（`len(open(f).readlines())`，含空行、含结尾空行）。
  ⚠️ 别用 PowerShell `Measure-Object -Line`（**只数非空行**，同文件可差 10~24%）——实测 `orders.py` 总行 1023 / 非空 929。
  ⚠️ **也别用 `(Get-Content $f).Count`**：它在部分文件上同样不可靠（实测 `data_retention.py` 给 115，真值 121）。**统一用 Python `readlines()`。**
- **修订记录（只记教训，不记流水）**：初版 2026-09-14，此后经 **3 轮独立复核、共修 19 处**。三条可复用的教训：
  1. **行数口径会骗人**：初版用 PowerShell `Measure-Object -Line`（只数非空行），全部行数偏低；换成 `(Get-Content).Count` 后仍有文件错（`data_retention.py` 给 115，真值 121）。**换了个"更可靠"的方法 ≠ 方法可靠**，现在统一用 Python `readlines()`。
  2. **引用校验通过 ≠ 内容正确**：第二轮 6 项、第三轮 4 项问题里，**没有一项是 `check_refs` 能查出来的**——全是"文件存在、但结论不对"。机械校验与独立复核不可互相替代。
  3. **约定的"元信息"也要被核对**：本表曾把 `L###` 的语义写反（声称指 `def` 行，实测一个都不是）。**写给读者看的规则，本身也需要被验证。**
