  69 |                 "type「$raw」不认。只能填 piece（按单计费的运费账单）或 salary（固定工资的月薪单）。",
  85 |             ?: throw AiWriteArgException("没找到司机「$name」。请让用户确认名字，不要自己挑一个。")
 140 |                 "系统里没有**工资制**司机（没有谁的计费方式是「固定工资」且月薪大于 0）。" +
 147 |                 "「$driverName」不在工资制司机名单里（可能是按单计费的司机）。" +
 181 |                 add("⚠️ 司机账单生成后**没有删除入口**（App 里翻不到「删账单」），月份请再核对一遍")
 249 |                 add("张数写「至多」是因为该月**未定价**的单不会补（它们没有金额可入账）")
 250 |                 add("⚠️ 司机账单生成后**没有删除入口**（App 里翻不到「删账单」），月份请再核对一遍")
 293 |  * "造一张废单「的能力，所以这里**根本不传 amount**，金额由后端按待结算明细汇总。
 311 |             ?: throw AiWriteArgException("没找到司机「$driverName」。")
 319 |                         "这个月该司机还没有账单——要先「生成司机账单」吗？（要用 settlements.generate_bills）"
 396 |             ?: throw AiWriteArgException("没找到司机「$driverName」。")
 404 |                     "结算单要先「生成结算单」（settlements.create）——请如实告诉用户这一步还没有做。",
 413 |                     "它当前状态是「${all.first().statusLabel()}」，而这个动作只对「" +
 414 |                     (if (requiredStatus == "draft") "草稿" else "已确认") + "」生效。" +
 420 |                     "请让用户到「司机运费结算」页面上指定是哪一张：\n" +
 467 |                 add("付款是**下一步**：确认之后还要再点一次「付款」，钱才会出账")
 468 |                 add("这些账单会从「待结算」变成「已结算」，司机端同步可见")
 509 |                 "付款方式「$raw」不认。只能填 cash（现金）/ transfer（转账）/ wechat（微信）/ bank（银行）。",
 577 |                 add("这张草稿作废，列表里会留下一条「已作废」的记录")
 617 |                 hint = "必填 YYYY-MM（如 2026-09）。**不要默认成「这个月"**，先问清楚",
 638 |                     hint = "必填 YYYY-MM（如 2026-09）。**不要默认成「这个月"**——月末和月初的人要的不是同一个月",
 666 |             blurb = "月末收口第三步：确认一张**草稿**结算单，把所属账单锁成「已结算」。" +